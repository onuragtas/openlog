import { PrintDashboardView } from "@/components/dashboards/PrintDashboardView";

/** Report print view loaded by openlog-renderer with a render token in the URL fragment (D-097). */
export function PrintDashboardPage() {
  return <PrintDashboardView />;
}
