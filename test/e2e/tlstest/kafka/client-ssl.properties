# Kafka CLI client over the mutual-TLS listener (health check).
security.protocol=SSL
ssl.keystore.type=PEM
ssl.keystore.location=/certs/client.pem
ssl.truststore.type=PEM
ssl.truststore.location=/certs/ca.crt
