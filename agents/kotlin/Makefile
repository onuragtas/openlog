# Mobile SDK for the JVM (io.openlog). The Gradle wrapper pins the build itself, so there is nothing to
# install first.
.PHONY: build test clean

build:
	./gradlew --no-daemon --console=plain assemble

test:
	./gradlew --no-daemon --console=plain test

clean:
	./gradlew --no-daemon --console=plain clean
