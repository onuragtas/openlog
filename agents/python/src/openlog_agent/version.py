# Every component shares the product SemVer (D-025); the release job writes the tag version here.
# Reported as telemetry.distro.version.
__version__ = "0.1.9"

#: telemetry.distro.name
DISTRO_NAME = "openlog"
