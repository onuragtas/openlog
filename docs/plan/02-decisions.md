# 02 — Karar Günlüğü

Her karar: tarih, karar, gerekçe, durum. Bir karar değişirse eski kayıt silinmez; yeni kayıt eklenir ve eskisi "yerine geçildi" olarak işaretlenir.

| # | Tarih | Karar | Gerekçe | Durum |
|---|---|---|---|---|
| D-001 | 2026-09-13 | Platform **sıfırdan** yazılır (Elastic/OTel backend'i üzerine kurulmaz). | Ürünün tamamının kontrolü ve SaaS + self-hosted esnekliği. | Kabul |
| D-002 | 2026-09-13 | Wire protokolü **OTLP** (protobuf, HTTP ve gRPC). | Kendi protokolümüz fayda getirmez; OTel SDK'ları agent'ımız olmayan dillerde de ilk günden çalışır. | Kabul (şimdilik) |
| D-003 | 2026-09-13 | **İlk agent: Linux infra agent** (makinenin kendisi). | Uygulama olmadan da değer üretir; ileride APM agent'ları için yerel forwarder olur. | Kabul |
| D-004 | 2026-09-13 | Lisans: agent'lar/SDK'lar **Apache-2.0**, backend + UI **AGPL-3.0**. | Agent'lar müşteri uygulamasına gömülür; backend'in kapalı SaaS olarak kopyalanması engellenir. | Kabul |
| D-005 | 2026-09-13 | İlk platform **Linux**. | Sunucu pazarının büyük çoğunluğu. | Kabul |
| D-006 | 2026-09-13 | Mimari **yatay ölçeklenen cluster** olarak çalışmak zorunda. Hiçbir serviste yerel state yok. | Yüke göre ölçekleme zorunlu gereksinim. | Kabul |
| D-007 | 2026-09-13 | Backend servisleri **Go**, UI **React + TypeScript**. | Tek binary, performans, iki kurulum profiline uygun. | Kabul |
| D-008 | 2026-09-13 | Telemetri deposu **ClickHouse** (Replicated + Distributed), metadata **PostgreSQL**. | Trace/log/metrik tek motorda; sıkıştırma, TTL, S3 tiering hazır. | Kabul |
| D-009 | 2026-09-13 | Kuyruk **Apache Kafka (KRaft)**; kod yalnızca Kafka protokolünü kullanır. Her profilde kuyruk vardır. | Tek kod yolu; Redpanda BSL lisansı AGPL ürünle varsayılan olarak dağıtılmaz ama kullanıcı bağlayabilir. | Kabul |
| D-010 | 2026-09-13 | SaaS ve büyük self-hosted kurulum **aynı Helm chart'ı** kullanır. | Cluster modu sürekli production'da sınanır. | Kabul |
| D-011 | 2026-09-13 | Infra agent kurulduğu makinedeki her şeyi **genel, kural tabanlı otomatik keşif** ile tanır; belirli bir ortama özel kod yazılmaz. | Ürün her müşteride sıfır konfigürasyonla değer üretmeli. | Kabul |
| D-012 | 2026-09-13 | Ürün adı (çalışma adı) **rapm**. | — | Yerine geçildi (D-013) |
| D-013 | 2026-09-13 | Ürün adı **openlog**; tek repo **github.com/onuragtas/openlog**. Agent'lar aynı repoda `agents/<ad>` altında, her biri kendi Go modülü ve Apache-2.0 `LICENSE` dosyasıyla. | Tek yerde sürümleme ve sözleşme değişikliklerinin agent ile backend'de aynı PR'da yapılabilmesi. Lisans ayrımı klasör bazında korunur. | Kabul |
| D-014 | 2026-09-13 | Ingest hata kodları: Kafka'ya yazılamaz/zaman aşımı → `503` / `UNAVAILABLE` + `Retry-After`; `429` / `RESOURCE_EXHAUSTED` yalnızca tenant limit aşımı (M1) için ayrılır. | Agent'ın "sunucu geçici olarak yok" ile "sen çok gönderiyorsun" durumlarını ayırt edebilmesi. | Kabul |
| D-015 | 2026-09-13 | ClickHouse veritabanı adı sabit: `openlog` (yapılandırılamaz). | Şema dosyaları ve Distributed tablo tanımları veritabanı adını içerir; değiştirilebilir olması hata kaynağıdır. | Kabul |
| D-017 | 2026-09-13 | UI: **React + TypeScript + Vite** SPA; TanStack Router + Query, shadcn/ui (Radix + Tailwind), uPlot (zaman serisi), Apache ECharts (diğer grafikler), React Flow (service map), TanStack Table/Virtual, CodeMirror 6 (sorgu editörü), i18next (TR/EN), Vitest + Playwright. Build çıktısı `go:embed` ile `openlog-api` binary'sine gömülür; SaaS'ta CDN'den de sunulabilir. | Ayrı Node sunucusu yok; self-hosted tek binary; gözlemlenebilirlik ekosistemi React ağırlıklı; dağıtılan paketin tüm kütüphaneleri MIT/Apache/BSD/ISC (AGPL uyumlu). Yalnızca build sırasında çalışan `lightningcss` (Tailwind/Vite bağımlılığı) MPL-2.0'dır; pakete girmediği için kabul edildi. | Kabul |
| D-018 | 2026-09-13 | Processor M1'de ClickHouse'a **Distributed tablo yerine doğrudan shard local tablolarına** yazar (shard'ı kendisi hesaplar, shard başına dedup token). | kind ölçümü: 2+ processor'da darboğaz Distributed insert koordinasyonu; processor ölçeklemesi ×1,4'te durdu. | Uygulandı (varsayılan `OPENLOG_PROCESSOR_INSERT_MODE=direct`). Tek dizüstünde tek processor ile verim aynı (~248k satır/s), koordinatör adımı kalktı ve yazmalar tüm sunuculara dağıldı; çok processor + çok node ölçümü tekrarlanacak. |
| D-019 | 2026-09-13 | Ingest `/readyz`, gerekli Kafka topic'leri yoksa **hazır değil** döner. | kind'da yeniden kurulumda topic'ler oluşmadan tüm pod'lar Ready oldu ve her istek 503 aldı. | Kabul |
| D-020 | 2026-09-13 | Processor bellek güvenliği: sınırlı Kafka fetch boyutu, bayt tabanlı sınırlar (`OPENLOG_PROCESSOR_BATCH_BYTES`, `OPENLOG_PROCESSOR_MAX_BUFFERED_BYTES`; ilk sürümdeki satır sayısı sınırının yerine), `GOMEMLIMIT` pod limitinden; processor için en az 2 GiB. Tüm retry'larda jitter. | kind'da processor birikmiş backlog'da OOM döngüsüne girdi; 4 processor aynı anlarda retry ederek ClickHouse'u 17 dk kilitledi. | Kabul (uygulandı) |
| D-021 | 2026-09-13 | **Ingest license key doğrulaması:** PostgreSQL + pod içi cache (bilinen key 60 sn, bilinmeyen 10 sn, key başına tek sorgu). Postgres kesintisinde son 15 dk içinde görülen key'ler kabul edilmeye devam eder, görülmeyenler `503` + `Retry-After` alır. İptal edilen key cache süresi kadar (≤60 sn) çalışmaya devam edebilir. | Ingest hızlı ve Postgres'ten bağımsız kalmalı; agent'lar kesintide veri atmamalı, tekrar denemeli. | Kabul (uygulandı) |
| D-022 | 2026-09-13 | **Kimlik bilgileri ayrı:** ingest license key (`olk_…`) ve API key (`ola_…`, salt okunur viewer) birbirinin yerine geçmez; davet token'ı `oli_…`. Hepsi 192 bit rastgele, veritabanında SHA-256 hash; yalnızca oluşturma yanıtında bir kez gösterilir. | Agent makinelerine dağıtılan key sızarsa veri okunamasın; programatik okuma yazma yetkisi taşımasın. | Kabul (uygulandı) |
| D-023 | 2026-09-13 | **Kullanıcı oturumu:** e-posta + parola (argon2id, 19 MiB, t=2, p=1), sunucu tarafında PostgreSQL'de tutulan oturum; cookie `HttpOnly; SameSite=Strict; Path=/api; Secure`; 7 gün ömür, 24 sa hareketsizlik; cookie ile yapılan değiştirici isteklerde `X-CSRF-Token`. Başarısız giriş sınırı e-posta+IP başına PostgreSQL'de (pod'lar arasında ortak). Roller: owner, admin, member, viewer. SSO/OIDC M4. | API pod'ları stateless kalır; anında iptal edilebilir oturumlar; OWASP önerilerine uygun parola hash'i. | Kabul (uygulandı) |
| D-024 | 2026-09-13 | **Organizasyon seçimi** istek başına `X-Openlog-Org-Id` header'ı ile; yoksa kullanıcının en eski üyeliği. | URL yapısını tenant'a bağlamadan çoklu organizasyon desteği. | Kabul (uygulandı) |
| D-025 | 2026-09-13 | **Tek ürün sürümü (app version):** backend, UI, Helm chart, imaj ve tüm agent'lar aynı SemVer ile birlikte yayınlanır; `stable`/`beta` kanalları; backend son 3 minor agent sürümünü destekler; backend `N` ve `N+1` aynı anda çalışabilir. | Kullanıcı tek sürüm görür; uyumluluk tek kuralla yönetilir; rolling update mümkün olur. | Kabul |
| D-026 | 2026-09-13 | **İmzalı release manifest'i:** Ed25519 imzalı `manifest.json` + `index.json`; açık anahtarlar binary'lere gömülü (2 anahtar rotasyon); artifact'lar sha256 ile manifest'e bağlı; sunucu güvenilmez kabul edilir. | Otomatik güncelleme saldırı yüzeyidir; backend ele geçirilse bile imzasız kod çalıştırılamamalı. | Kabul |
| D-027 | 2026-09-13 | **Agent kendini günceller:** ingest üzerinden `sync`, `versions/<v>` + `current` symlink, `-self-test`, atomik geçiş, onaylanmazsa otomatik geri dönüş; container kurulumları kendini güncellemez. | Binlerce makinede elle güncelleme olmaz; bozuk sürüm filoyu düşürmez. | Kabul |
| D-028 | 2026-09-13 | **Filo politikası:** organizasyon başına `off/notify/auto`, `latest/patch/pinned`, kademeli dalgalar (`hash(host.id)`), başarısızlık oranında otomatik durdurma, bakım penceresi, host bazında hold/pin; varsayılan `auto` + `10/50/100`. | Güvenli ve kontrollü dağıtım; hatalı sürüm küçük bir dalgada yakalanır. | Kabul |
| D-029 | 2026-09-13 | **Backend güncelleme:** expand/contract migration fazları ve `N`/`N+1` birlikte çalışma testi; self-hosted için imzalı sürüm kontrolü + UI bildirimi, Compose'da opsiyonel `openlog-updater` (yedek → migrate → yeniden başlat → sağlık kontrolü → geri dönüş), Kubernetes'te GitOps önerisi + opsiyonel updater CronJob. | Veritabanı olan bir sistemin otomatik güncellemesi veri kaybı riskini sıfırlamalı. | Kabul |
| D-030 | 2026-09-13 | **Alarm:** `openlog-alert` stateless + Postgres lease ile kural paylaşımı; tenant-scoped sorgu katmanı; metrik/log/veri yok/APM/keşif kural tipleri; `ok→pending→firing→resolved`; incident; Slack/e-posta/webhook/Teams; idempotent teslim. Ayrıntı: 10-m2 §1. | New Relic'in temel değeri sorunu haber vermek; cluster kuralı (D-006) korunmalı. | Kabul |
| D-031 | 2026-09-13 | **Entegrasyonlar** infra agent modülü, keşifle açılır, kimlik bilgisi gönderilmez, metrik isimleri OTel Collector receiver'larıyla aynı. | Tek agent; OTel Collector verisiyle uyumlu ekranlar. | Kabul |
| D-032 | 2026-09-13 | **APM:** OTLP span'larından processor + ClickHouse MV ile RED/Apdex/edge/DB/hata grupları; transaction = servis giriş span'ı; servis haritası dakikalık eşleştirme işiyle. | Sorgu anında ham span taraması ölçeklenmez. | Kabul |
| D-033 | 2026-09-13 | **Go agent:** OTel Go SDK üzerine openlog dağıtımı (`agents/go`, Apache-2.0). | Go'da kod değiştirmeden ölçüm sınırlı; standarda uyum. | Kabul |
| D-034 | 2026-09-13 | **PHP agent:** M2'de iki yolun (kendi extension vs OTel extension) ölçülü karşılaştırması + prototip; nihai karar kullanıcıya. | Yüksek maliyet/risk; veriye dayalı karar. | Yerine geçildi (D-035) |
| D-035 | 2026-09-13 | **PHP agent = kendi C extension'ımız** (`agents/php`, Apache-2.0). OTel PHP extension yolu kullanılmaz. | Ölçüm (`agents/php/docs/decision.md`): kendi extension prototipi ölçülebilir ek yük getirmedi (RPS 1179 / ölçümsüz 1159, CPU değişmedi) ve backend kapalıyken hatasız çalıştı; OTel yolu kapasiteyi %61 düşürdü, CPU'yu %92 artırdı, backend kesintisinde 16 RPS'e çöktü. | Kabul |
| D-036 | 2026-09-13 | **PHP sürüm desteği 7.1 ve üstü.** PHP ≥ 8.0'da Observer API, 7.1–7.4'te `zend_execute_ex`/`zend_execute_internal` kancaları; tek extension kaynak kodu iki kanca katmanıyla. Derleme matrisi: her minor sürüm × NTS/ZTS × glibc/musl × amd64/arm64. | Kullanıcının müşteri tabanında eski PHP projeleri yaygın. | Kabul |
| D-037 | 2026-09-13 | **Derinlik:** route/transaction, hata, PDO/mysqli/Redis, dış HTTP (curl/Guzzle, `traceparent` enjeksiyonu) + **fonksiyon seviyesi ayrıntı** (yalnızca eşik üstü yavaş transaction'larda, segment sayısı/süre limitleriyle; New Relic "transaction trace" benzeri). | New Relic benzeri tanı derinliği isteniyor; ek yük eşik ve limitlerle sınırlanır. | Kabul |
| D-038 | 2026-09-13 | **Forwarder infra agent içinde modül:** extension span'ları engellemesiz yerel sokete yazar, infra agent toplar ve OTLP ile ingest'e gönderir (disk buffer, yeniden deneme, aynı otomatik güncelleme). | Ayrı kurulum yok; backend kesintisinde PHP isteği etkilenmez; tek güncelleme yolu. | Kabul |
| D-039 | 2026-09-13 | **Uzaktan entegrasyon yapılandırması:** entegrasyon ayarları (endpoint, kullanıcı, parola, veritabanı, açık/kapalı) UI'dan organizasyon genelinde veya host bazında girilir, PostgreSQL'de saklanır (parola `OPENLOG_SECRETS_KEY` ile AES-GCM şifreli, API hiçbir zaman geri döndürmez, yalnızca `password_set`), agent'a mevcut `sync` yanıtında revizyonlu (`integrations_config_revision`) olarak iletilir; değişmediyse gönderilmez. Agent `config.yaml` üzerine alan bazında uygular (uzaktaki kazanır), son yapılandırmayı state dizininde 0600 saklar, yeniden başlatmadan uygular. `integrations.remote_config: false` ile kapatılır. D-031'deki "kimlik bilgisi gönderilmez" kuralı agent → backend yönü için geçerli kalır. | Kullanıcıların SSH ile `config.yaml` düzenlemesi gerekmemeli; yeni bağlantı veya port açılmaz (agent zaten sync yapıyor); sırlar yalnızca TLS ingest bağlantısında düz metin. Yerel yönetimi tercih eden kurulumlar tek anahtarla dışarıda kalır. | Kabul |
| D-040 | 2026-09-13 | **Docker erişimi varsayılan açık:** deb/rpm ve `install.sh`, `docker` grubu varsa `openlog-agent` kullanıcısını gruba ekler (konteyner metrikleri, konteyner port/IP keşfi). Kapatma: `install.sh --no-docker-access`, `OPENLOG_AGENT_DOCKER_ACCESS=0` veya `/etc/openlog-infra-agent/no-docker-access` dosyası (güncellemelerde de korunur). Kurulum çıktısı grubun root'a eşdeğer olduğunu açıkça yazar. Docker API'si yine de okunamazsa agent konteyner portlarını `/proc/<pid>/net` ve `docker-proxy` üzerinden türetir. | Konteynerde çalışan servislerin sıfır konfigürasyonla izlenmesi (D-011); root eşdeğeri yetki bilinçli bir tercih olduğundan tek adımla kapatılabilir ve kalıcıdır. | Kabul |
| D-041 | 2026-09-14 | **Manuel güncelleme istek kanalı:** UI'daki "Şimdi kontrol et" / "Şimdi güncelle" düğmeleri `openlog-updater` ile doğrudan konuşmaz; api PostgreSQL'deki `update_requests` tablosuna satır ekler (admin/owner, CSRF, audit, kurulum başına eylem türü başına 30 sn'de bir, `OPENLOG_SIGNUP_ENABLED=true` iken kapalı). Compose updater tabloyu `OPENLOG_UPDATER_REQUEST_POLL` (10 sn) aralığıyla `FOR UPDATE SKIP LOCKED` ile okur ve `updater_poll` kalp atışı yazar; Kubernetes CronJob'u bekleyen istekleri bir sonraki çalışmasında işler. `apply` isteği `notify` modunda da uygular, yalnızca onaylanan sürüm hâlâ seçilen sürümse; bakım penceresi dışında yalnızca açık onayla. Agent tarafında "Şimdi dağıt" dalga beklemelerini atlar; dalga bekleyen agent'lara sync yanıtında 60 sn `poll_interval_seconds` verilir. | Her iki updater zaten status belgesi için aynı veritabanına bağlı: yeni port, servis, token veya Kubernetes RBAC (CronJob'dan Job oluşturma) gerekmez; api pod'u yeniden oluşturulurken istek kaybolmaz; pod'lar arası ortak hız sınırı ve tek açık `apply` veritabanında garanti edilir. LISTEN/NOTIFY bağlantı kopmalarında olay kaybettirdiği ve yine yoklama gerektirdiği için seçilmedi; 10 sn'lik yoklama kısmi index üzerinde boşta yazma yapmaz. Bedeli: Kubernetes'te anlık değil (CronJob takvimi), eski updater'lar isteği görmez (15 dk'da `expired`). | Kabul (uygulandı) |
| D-042 | 2026-09-14 | **Agent güncellemesi paket kurulumuyla eşdeğer, root adımı ayrı:** agent (`openlog-agent`) yeni sürümü yalnızca doğrulayıp `/var/lib/openlog-infra-agent/updates/<v>/` altına hazırlar; unit'in `ExecStartPre=-+…/current/openlog-infra-agent -apply` adımı root olarak imzayı kendi gömülü anahtarlarıyla yeniden doğrular, arşivi kopyalayıp sha256'yı kopyada kontrol eder, root sahipli kurar, adayın self-test'ini `openlog-agent` olarak çalıştırır, `current`'ı değiştirir, başarısız başlatmalarda kendi kaydettiği önceki sürüme döner ve yeni sürümün `-reconcile` adımını çalıştırır (gömülü systemd unit, kullanıcı, docker grubu, sahiplik/izinler). Aynı `-reconcile` deb/rpm postinstall ve `install.sh` tarafından da çağrılır. `/opt/openlog/infra-agent` root sahiplidir, agent yazamaz. Unit değiştiyse agent o başlatmada bir kez çıkar. Eski unit'li hostlar bir kez paket yükseltmesi veya `install.sh` ister; o zamana kadar yalnızca binary güncellenir ve `update_notice: unit outdated` raporlanır. | Yalnızca binary değişince unit düzeltmeleri (ör. SIGSYS), docker grubu ve sahiplik değişiklikleri kendini güncelleyen hostlara hiç ulaşmıyordu. Agent'ın yazabildiği bir binary'yi root olarak çalıştırmak (`ExecStartPre=+` + agent sahipli `versions/`) agent kullanıcısını ele geçireni root yapardı; bu yüzden root adımı yalnızca root sahipli dosyaları çalıştırır ve state dizinindeki her şeyi güvenilmez girdi sayar. | Kabul (uygulandı) |
| D-043 | 2026-09-14 | **Konteynerler birinci sınıf varlık:** infra agent her metrik aralığında konteyner başına `openlog.container.status` (durum, sağlık, başlama zamanı) ve `container.restarts` gönderir; tüm konteyner metriklerine Docker etiketlerinden `docker.compose.project/service` ve `k8s.*` adları eklenir. ClickHouse'ta `containers` ve `apm_service_containers` tabloları processor koduyla değil, `apm_service_hosts` gibi **materialized view** ile (`metrics_local` / `spans_local` üzerinde, tüm kolonlar birleştirilebilir `min/max/argMax`) doldurulur; durum alanlarında durum noktası dışındaki satırlar epoch sürümüyle yazılır, böylece en son durum her zaman kazanır. Servis ↔ konteyner bağı span resource'undaki `container.id` ile kurulur; `host.id` + süreçle eşleştirme yapılmaz. Konteyner logları (`logs.containers`, varsayılan açık) json-file dosyası okunabiliyorsa (unit'teki `CAP_DAC_READ_SEARCH`) dosyadan, değilse Docker API akışından (`docker` grubu) toplanır; kayıtlar konteynerin resource'uyla gönderilir, `logs` tablosuna `container.id` ve compose servisi için bloom filter indeksleri eklenir. | Varlık satırları D-018 doğrudan shard yazımıyla ve yeniden teslimle bozulmamalı (MV + birleştirilebilir kolonlar tekrar eden blokta aynı sonucu verir; ReplacingMergeTree'de metrik satırı durum satırını ezerdi); ayrı bir Kafka kaydı veya processor tablosu gerekmez. Dosyadan okuma Docker daemon'una yük bindirmez ve ofsetler mevcut dosya altyapısıyla korunur; API yolu `local`/`journald` sürücülerini ve DAC yetkisi olmayan kurulumları kapsar. Unit zaten `CAP_DAC_READ_SEARCH` verdiği için yeni yetki eklenmedi. Bedeli: konteyner başına her aralıkta 2 ek seri; containerd/CRI-O konteynerleri adsız kalır; çok satırlı gruplama konteyner loglarında yok. | Kabul (uygulandı) |
| D-047 | 2026-09-14 | **Tenant sorguları salt okunur ClickHouse kullanıcısıyla ve tenant başına limitlerle:** api ve alert tenant sorgularını (`internal/api/query`) ayrı bir bağlantıyla `OPENLOG_CLICKHOUSE_READ_USER` olarak çalıştırır; kullanıcının profili `readonly=2` (yalnızca okuma, ayar değiştirilebilir), yetkisi yalnızca `GRANT SELECT ON openlog.*`. Yazıcı kullanıcı migrate, processor, APM edge linking ve alarm değerlendirme geçmişinde kalır. Okuyucu tanımlı değilse yazıcıya düşülür ve uyarı loglanır. Compose kullanıcıyı `users.d` XML'iyle (parola `from_env`), Helm CHI `users`/`profiles` ile oluşturur. Her sorgu `max_execution_time`, `max_memory_usage` (varsayılan 2 GiB), `max_rows_to_read` (2·10⁹), `max_bytes_to_read` (kapalı), `log_comment` = `{"component","tenant_id"}` ve `quota_key` = tenant ayarlar; tenant bazlı istisnalar `OPENLOG_QUERY_TENANT_LIMITS` env'i ile. Limit aşımı (241/158/307/396) `422 resource_exhausted`, kota/eşzamanlılık/sunucu belleği (201/202/241 total) `429` + `Retry-After`. | Sorgu katmanındaki tenant filtresi (tenant_id parametresi) bir yazılım sınırı; api süreci ele geçirilse veya builder'da bir açık bulunsa bile ClickHouse tarafında yazma, DDL, başka veritabanı ve `system` erişimi engellenmeli. `readonly=1` limit ayarlarının sorgu başına gönderilmesini engellediği için 2 seçildi (bedeli: kullanıcı limitleri yükseltebilir; gerekirse profile `constraints` eklenir). Limitler bir tenant'ın ağır sorgusunun shard'ların belleğini tüketmesini önler; `quota_key` ClickHouse kotalarını tenant bazında kullanılabilir kılar. PostgreSQL'de org ayarları deposu olmadığı için istisnalar şimdilik env'de; ayar deposu gelince taşınır. | Kabul (uygulandı) |
| D-048 | 2026-09-14 | **Sertifikalar yeni bağlantıda dosyadan yeniden yüklenir:** Kafka, ClickHouse (bootstrap + shard/replica havuzları) ve PostgreSQL istemcileri TLS'i statik `tls.Config` yerine bağlantı başına kurar: CA/sertifika/anahtar dosyalarının içerik özeti en çok 10 sn'de bir yeni bağlantı açılırken kontrol edilir, değiştiyse yapılandırma yeniden kurulur (Kafka `kgo.Dialer`, ClickHouse `DialContext`, PostgreSQL `pgxpool.BeforeConnect` ile DSN yeniden ayrıştırılır). Yüklenemeyen dosya seti (döndürme ortasında) önceki sertifikaları korur ve bir kez loglanır. Açık bağlantılar kendi sertifikalarıyla kapanana kadar sürer. ingest/api TLS sonlandırmadığı için sunucu sertifikası yenileme yok. | cert-manager/Secret döndürmesinde pod yeniden başlatmak gerekmemeli. fsnotify yerine lazy içerik özeti: Kubernetes Secret volume'ları symlink değiştirerek güncellenir (mtime/inotify güvenilmez), ek bağımlılık ve goroutine gerekmez, maliyet yalnızca yeni bağlantıda küçük dosya okuma. Doğrulamayı kendimiz yazmak (`VerifyConnection` + CA havuzu değişimi) yerine her bağlantıda yeni `tls.Config` kullanmak sürücülerin kendi hostname/libpq `sslmode` doğrulamasını korur. | Kabul (uygulandı) |
| D-049 | 2026-09-14 | **Operator modunda ClickHouse sunucu TLS'i chart'ta:** `clickhouse.tls.server.secretName` (cert-manager Secret'ı: `tls.crt`, `tls.key`, `ca.crt`) verilince CHI'ye `tcp_port_secure` + `https_port`, `config.d/openlog-tls.xml` (openSSL server ve shard'lar arası client), Secret volume'u (`fsGroup` 101, mod 0440), cluster `secure: "yes"` (remote_servers güvenli port, `<secure>1</secure>`) ve Service portları eklenir; `disableInsecure` ile `insecure: "no"`. openlog pod'ları ayrı `caSecret` yoksa bu Secret'ın CA'sına güvenir; adres `clickhouse.tls.enabled` ile güvenli porta döner. Keeper ve replikasyon (interserver HTTP) TLS'i kapsam dışı. | Önceki durumda operator modunda istemci TLS'i vardı ama sunucu tarafını kullanıcı elle `settings`/`files` ile kurmak zorundaydı; doğrudan shard bağlantıları (D-018) ve Distributed sorgular için remote_servers'ın da güvenli porta geçmesi gerekiyor ve bu operator'ın `secure` bayrağıyla tutarlı yapılabiliyor. Sertifikalar pod volume'undan okunur (CHI `files` yerine) ki Secret yenilemesi pod yeniden oluşturmadan dosyalara yansısın. Bedeli: sertifika tüm pod hostlarını (`*.<ns>.svc.cluster.local`) ve clientAuth kullanımını kapsamalı; `disableInsecure` operator'ın HTTPS erişim ayarını ister. | Kabul (uygulandı; kind'de 2026-09-14 strict doğrulamayla denendi, `disableInsecure` denenmedi; Keeper/replikasyon TLS'i D-093) |
| D-044 | 2026-09-14 | **Key hash'lerinde sunucu tarafı gizli anahtar:** `OPENLOG_KEY_HASH_SECRET` verilince ingest license key ve API key'ler `HMAC-SHA256(secret, key)` olarak saklanır (yoksa eskisi gibi SHA-256). Arama tek sorguda tüm aday hash'lerle yapılır (güncel HMAC, `_PREVIOUS` HMAC, SHA-256; `ORDER BY array_position`) ve eski biçimle bulunan aktif satır aynı ifadede güncel hash'e yeniden yazılır (veri değiştiren CTE, hash zaten varsa atlanır). Ingest bunu yalnızca cache miss'te yapar; cache anahtarı bellekte SHA-256 kalır. İçe aktarılan/bootstrap key oluştururken eski biçim hash'i de "kullanımda" sayılır. Rotasyon `_PREVIOUS` ile; oturum/davet/doğrulama token'ları SHA-256 kalır. Migration yok. | Veritabanı dökümü tek başına, operatörün seçtiği düşük entropili içe aktarılmış key'leri çevrimdışı tahmin etmeye yetmemeli. Ayrı kolon + çift yazma yerine yerinde yeniden hash: şema değişmez, geçiş şeffaftır, ingest sıcak yolu değişmez. Bedeli: gizli anahtar kaybolursa key'ler geçersizleşir (düz metin yok); tüm servislerde aynı değer olmalı ve eski sürüm pod'lar yeniden yazılmış key'leri çözemeyeceği için gizli anahtar yükseltme tamamlandıktan sonra verilir. | Kabul (uygulandı) |
| D-045 | 2026-09-14 | **Davet e-postası ve kayıt koruması:** api, alarm kanallarının global SMTP sunucusunu (`OPENLOG_SMTP_*`) ve `OPENLOG_PUBLIC_URL`'i kullanarak davet ve e-posta doğrulama bağlantısı gönderir (`internal/mail`, senkron, 15 sn); ikisi yoksa davet bağlantısı UI'da kopyalanır. Davet yeniden gönderilebilir (yeni token + yeni süre, eski bağlantı geçersiz; süresi dolmuşlar dahil). Kayıt (`OPENLOG_SIGNUP_ENABLED`): IP başına ve e-posta başına deneme sınırı (sayaçlar `login_failures` tablosunda, pod'lar arası ortak), isteğe bağlı Cloudflare Turnstile/hCaptcha sunucu doğrulaması, engellenen e-posta alan adı listesi (env + dosya, alt alan adları dahil), parola politikası 8–256 ve e-postaya eşit olamaz. `OPENLOG_SIGNUP_REQUIRE_VERIFICATION` (e-posta yapılandırılmışsa varsayılan açık): doğrulanmamış kullanıcı giriş yapar ve okur ama license/API key oluşturamaz, davet gönderemez. E-posta gönderim sınırları: org başına saatte 100, davet adresi başına 5, kullanıcı başına 5 doğrulama e-postası. | SaaS'ta bedava hesap açıp veri gönderme veya davet e-postasıyla spam yapma kapatılmalı; yeni tablo/servis olmadan (sayaçlar mevcut tabloda, SMTP mevcut yapılandırmada) çalışır. Kuyruk yerine senkron gönderim: M1 hacminde yeterli ve hata admin'e hemen görünür (bağlantı yine döner). Engelleme listesi dosyadan: tek dağıtık liste yok, operatör seçer. | Kabul (uygulandı) |
| D-046 | 2026-09-14 | **Üyelikten çıkarma anında etkili:** oturum cache'lenmez; her istekte oturum + seçili organizasyondaki üyelik/rol PostgreSQL'den okunur (istek başına mevcut 2 indeksli sorgu). Bu yüzden üye çıkarıldığında veya rolü değiştiğinde tüm api pod'larında bir sonraki istek yeni durumu görür (sürüm sayacı/TTL gerekmez). Üye çıkarılınca o organizasyonda oluşturduğu API key'ler de iptal edilir; kullanıcının diğer organizasyonlardaki oturumu sürer. | "Rol yok" durumu üyeliğin silinmesidir ve zaten istek başına denetleniyordu; cache + geçersizleştirme karmaşıklık ve pencere yaratırdı. API key'ler organizasyona bağlı olduğu için çıkarılan kişinin script'leri veri okumaya devam ederdi. Bedeli: yüksek istek hacminde Postgres yükü (gerekirse ileride kısa TTL'li, sürüm sayaçlı cache). | Kabul (uygulandı) |
| D-054 | 2026-09-14 | **Geç span'lerin yeniden bağlanması:** processor, `OPENLOG_APM_RELINK_AFTER`'dan (varsayılan: link lookback) geç gelen span'lerin client dakikalarını ClickHouse `apm_relink_queue` tablosuna (schema 0020, MergeTree, 8 gün TTL) yazar; api lideri tabloyu Distributed üzerinden bellekteki watermark ile (2 dk bekleme, 10 dk örtüşme, lider değişiminde 1 sa yeniden okuma) okur ve her `OPENLOG_APM_RELINK_INTERVAL`'da en fazla `OPENLOG_APM_RELINK_MAX_MINUTES` dakikayı tüm shard'larda yeniden bağlar; catch-up ile aynı anda çalışmaz. | Günlük catch-up'tan sonra gelen span'ler hiç bağlanmıyordu. Shard başına kuyruk satırı iz başına satır gerektirirdi (backlog'da patlar); processor'ın Postgres bağlantısı yok. Yeniden bağlama idempotent (ReplacingMergeTree). | Kabul (uygulandı) |
| D-055 | 2026-09-14 | **Önerilen alarm şablonları sunucuda:** şablon kataloğu Go'da (`internal/alert/templates.go`), `GET /alerts/templates` ile tr/en ad, açıklama ve parametre tanımlarıyla sunulur; `POST /alerts/templates/{id}/render` doğrulanmış `RuleInput` üretir (etiket `openlog.template`), kayıt yapmaz; UI mevcut önizleme ve kural oluşturma uç noktalarını kullanır. Oran eşikleri (Redis maxmemory, PostgreSQL max_connections) render anında örneğin son değerinden sayıya çevrilir. | Şablonlar API kullanıcılarına ve ileride Terraform'a da açık olmalı; eşik/metrik bilgisi kural doğrulamasıyla aynı yerde test edilir; UI'daki istemci tarafı ön ayarlar kaldırıldı. Bedeli: oran eşiği sabit sayı olarak saklanır, sunucu ayarı değişirse yeniden oluşturmak gerekir. | Kabul (uygulandı) |
| D-056 | 2026-09-14 | **Susturma takvimleri:** tekrarlayan susturmalar RFC 5545 alt kümesi `FREQ=MONTHLY;BYMONTHDAY|BYDAY(sıralı)|BYSETPOS`, `exdates` (yerel tarih) ve adlandırılmış tatil takvimleri (`alert_holiday_calendars`, Postgres 0015; `YYYY-MM-DD` veya her yıl `MM-DD`; admin/owner yönetir) destekler. İstisna, o yerel tarihte **başlayan** tekrarı atlar. Takvimler şema JSON'unda id ile referanslanır ve yükleme anında çözülür; kullanımdaki takvim silinemez. | Ayın son iş günü, resmi tatiller gibi operasyon takvimleri haftalık günlerle ifade edilemiyordu. Tarihleri susturmaya kopyalamak takvim güncellemelerini yaymazdı. Bedeli: rolling upgrade sırasında eski dispatcher'lar aylık kuralları susturmaz ve istisna günlerinde susturur. | Kabul (uygulandı) |
| D-060 | 2026-09-14 | **Node.js agent:** OTel JS SDK üzerine openlog dağıtımı `@openlog/node` (`agents/node`, Apache-2.0, Node 18.19+/20/22/24). `node --require/--import @openlog/node/register` ile kodsuz başlatma veya `start()` API; ortam değişkenleri Go agent'la aynı (`OPENLOG_*` > `OTEL_*`); OTel paketleri tam sürümle sabitlenir; CommonJS derlemesi + aynı örneği yeniden dışa aktaran ESM cephesi (çift paket durumu yok). http/undici/express/fastify (`@fastify/otel`)/koa/NestJS 4–11/pg/mysql2/redis/ioredis/mongodb/graphql/grpc/aws-sdk, pino/winston log korelasyonu + OTLP log (console isteğe bağlı). Entry span'e alt span'lerdeki en uzun `http.route` kopyalanır; DB metni Go `openlogsql.Sanitize` ile aynı algoritmayla temizlenir. Runtime metrikleri semconv adlarıyla (`nodejs.eventloop.*`, `v8js.*`, aktif handle'lar `v8js.resource.active`, `process.cpu.time`, `process.memory.usage`), GC histogramı Go agent sınırlarıyla. | Node.js'te OTel standart yol; openlog'a özgü kısım (host.id zinciri, örnekleme, DB temizleme, route adlandırma) Go agent'la aynı sözleşmeyi uygular. NestJS 12 yalnızca ESM ve upstream instrumentation kapsamı dışında (HTTP span'leri express/fastify route'uyla adlanır). | Kabul (uygulandı) |
| D-061 | 2026-09-14 | **Diller arası örnekleme uyumu testle bağlanır:** Node örnekleyicisi Go `sampler.go`'nun birebir portu (`ot=th`, `sampling.ratio`, `ot=rv`, W3C sınırları); Go agent'ın örnekleyicisinden üretilen `traceparent`/`tracestate` fikstürleri (`agents/node/test/interop/go-sampler-fixtures.json`, `scripts/gen-go-fixtures.sh`, agents/go'yu değiştirmeden geçici kopyada) Node testlerinde birebir doğrulanır. JS SDK trace flag'lerini yalnız örnekleme kararından türettiği için W3C random bayrağı (0x02) tracer sarmalayıcısıyla span oluşturulduktan hemen sonra eklenir (kökte her zaman, çocukta ebeveynden). | İki agent'ın aynı izi farklı ağırlıklandırması APM sayılarını bozar; fikstürler davranış kaymasını CI'da yakalar. Bedeli: SDK iç span context nesnesinin değiştirilmesi (SDK sürüm yükseltmelerinde testle korunur). | Kabul (uygulandı) |
| D-062 | 2026-09-14 | **npm yayını ürün tag'inden:** `release.yml` `node-agent-npm` job'u GitHub release yayımlandıktan sonra `agents/node` sürümünü tag'den (`npm version X.Y.Z`) ayarlar, testleri çalıştırır ve `@openlog/node@X.Y.Z`'yi `--provenance` ile yayımlar (beta kanalı dist-tag `beta`); `NPM_TOKEN` secret'ı yoksa uyarıyla atlanır, aynı sürüm zaten varsa yeniden yayımlamaz. CI `node-agent` job'u Node 20/22'de lint, typecheck, birim + uçtan uca testleri, Node 22'de gerçek PostgreSQL/MySQL/Redis ile instrumentation testlerini çalıştırır. | Tek ürün sürümü (D-025); npm sürümü değiştirilemez, bu yüzden yayın ancak release başarıyla yayımlandıktan sonra yapılır. Depoya sürüm commit'i gerekmez (Go modüllerinin aksine npm paketi derlenmiş dosyalardan yayımlanır). | Kabul (uygulandı) |
| D-057 | 2026-09-14 | **PHP agent ek yükü: sıcak yol yeniden yazımı + yalın mod.** Metin kodu (JSON yazıcı, UTF-8 temizleme, SQL sanitize, yol normalleştirme, traceparent) PHP'den bağımsız `ol_text.c`'ye taşındı (tablo tabanlı karakter sınıfları, 8 baytlık taramalar; libFuzzer ile fuzz edilir); sorgu analizi istek başına sorgu metnine göre önbelleklenir; örnekleyici thread'i istekten sonra 200 ms uyanık kalır (arka arkaya isteklerde futex yok). Ölçüm (callgrind, `php-cgi -T`, istek başına komut): düz PHP (57 SQL) izleyici kapalı ek yük 1,12 M → 0,58 M; Laravel'de değişmedi (1,78 M) çünkü neredeyse tamamı motorun Observer API'sinin çağrı başına maliyeti (8.0–8.3 ~100, 8.4 ~50–65 komut/çağrı; gözlemci hiçbir şey yapmasa da). `openlog.userland_hooks=0` (yalın mod) fcall observer'ı / `zend_execute_ex`'i kaydetmez, iç fonksiyonları `zend_execute_internal` ile ölçer: Laravel ek yükü 0,27 M komut (%3,6); framework route adları, framework'ün raporladığı istisnalar, Predis ve worker transaction'ları yoktur. Varsayılan tam mod kalır. | Hedef (≤%3/≤%7) çağrı yoğun framework'lerde Observer API ile tutturulamıyor; bunu gösteren tekrarlanabilir ölçüm (`bench/ext/micro`, `bench/ext/http`) ve seçilebilir ucuz yol gerekli. Kullanıcı kancalarını opcode enjeksiyonu veya `zend_execute_ex` ile yapmak JIT'i kapatır ya da opcache/optimizer karşısında kırılgandır; reddedildi. | Kabul (uygulandı) |
| D-058 | 2026-09-14 | **Uzun ömürlü PHP worker'ları:** Laravel Octane (`Worker::handle`), RoadRunner (`HttpWorker::waitRequest` … `respond`) ve Swoole/OpenSwoole (`Server::on('request')` callback'i … `Response::end`) için işlenen istek başına bir transaction. Transaction yaşam döngüsü RINIT/RSHUTDOWN'dan ayrıldı (`ol_txn_start`/`ol_txn_finish`); bağlantı, statement ve curl handle durumu transaction arenası yerine istek ömürlü bellekte. Worker sürecinin kendi CLI transaction'ı atılır; istekler arasında kayıt ve header yayılımı yok. Aynı süreçte eşzamanlı istekler (Swoole coroutine'leri) karıştırılmaz: birden fazla istek sürerken alt span'ler, route adı ve fonksiyon izleyici hepsi için durur, her istek kök span'ıyla (`openlog.php.concurrent=true`) kaydedilir; giden `traceparent` çağrı yığınında handler çerçevesi bulunan isteğin bağlamını taşır, bulunamazsa hiç taşımaz. Ek (2026-09-14): FrankenPHP worker modu da kapsamda — `frankenphp_handle_request()`'e verilen callable'ın çağrısı istek başına bir transaction (istek bilgisi FrankenPHP'nin her istek için sıfırladığı `$_SERVER`/`SG(request_info)`'dan, durum callback dönüşündeki `http_response_code`), worker betiğinin kendi çalışması (`FRANKENPHP_WORKER`) hiç gönderilmez; klasik mod normal SAPI isteği. | Coroutine başına tam span ağacı tüm istek durumunun coroutine başına kopyasını gerektirir (Swoole coroutine geçişleri motor tarafından gözlenemiyor). Yanlış isteğe bağlanan span veya trace bağlamı sessiz veri bozulmasıdır ve eksik ayrıntıdan kötüdür. Yalın mod kapsam dışı (callable'lar userland gözlemiyle yakalanıyor). | Kabul (uygulandı) |
| D-059 | 2026-09-14 | **PHP agent dağıtımı:** mimari başına tek tarball + `openlog-php-agent` deb/rpm/apk; tüm ABI modülleri (`<API>-<nts\|zts>-<glibc\|musl>`, 7.1–8.4 → 36 modül) aynı pakette. glibc modülleri AlmaLinux 8'de (glibc 2.28'den yeni sembol sürümü yasak, derleme kontrol eder), musl modülleri resmi Alpine imajlarında derlenir; her modül resmi PHP imajında yüklenerek test edilir. `openlog-php-install` çalışma zamanlarını bulur (`<bin> -i`), modülü ini ile mutlak yoldan (`/opt/openlog/php-agent/current/modules/…`) yükler, `-m` ile doğrular, yüklenmezse geri alır; kendi yazmadığı ini'lere dokunmaz, `--reload` olmadan FPM/Apache'yi yeniden başlatmaz. Filo kurulumu infra agent'ın imzalı güncelleme hattı ile (manifest bileşeni `php-agent`, root adımı, sağlık kontrolü, geri dönüş) yapılır: `docs/contracts/php-agent.md` §7.3. | Resmi PHP imajları Debian trixie tabanlı; orada derlenen modül glibc 2.34+ sembolleri ister (ör. `pthread_create@GLIBC_2.34`) ve RHEL 8, Debian 11, Ubuntu 20.04'te yüklenmez. `current` bağlantısı sayesinde yükseltme ini dosyalarını değiştirmez ve çalışan süreçler eski dosyayı haritalı tutar. Paket kurulumu servisi yeniden başlatmaz (üretimde istenmeyen kesinti). | Kabul (artefakt, paketler ve kurucu uygulandı; infra agent tarafı ve release tool'un `php-agent` bileşeni yapılacak) |
| D-016 | 2026-09-13 | `listening_port` anahtarında IPv6 adresleri köşeli parantezle yazılır (`tcp:[::]:22`). | `tcp::::22` gibi anahtarlar belirsizdir. | Kabul |
| D-068 | 2026-09-14 | **APM hata gelen kutusu iş akışı ve parmak izi v2:** hata grubu durumu (`unresolved`/`resolved`/`ignored`), atanan kişi (organizasyon üyesi), yorumlar PostgreSQL'de (`apm_error_group_states`, `apm_error_group_comments`, migration 0025; satırı olmayan grup çözülmemiş sayılır), her değişiklik `audit_log`'a. Çözülmüş grup, `resolved_at`'ten sonra tekrar görülünce otomatik yeniden açılır; `resolved_in_version` V verilmişse yalnızca V'de ya da V'nin ilk göründüğü andan sonra ilk kez görülen sürümlerde (dağıtım sırası, semver değil) oluşan hatalar sayılır. Tespit arka plan işi değil: API her gelen kutusu/detay okumasında ve `apm_error` (`regressed`) alarm kuralları her değerlendirmede yapar; geçiş koşullu UPDATE ile bir kez kalıcılaşır. Sürüm/host/container/transaction boyutları ClickHouse `apm_error_group_dims` MV'sinde (schema 0035). Mesaj normalizasyonuna ISO zaman damgaları ve `önek_<id>` kimlikleri, frame anahtarına Go func literal/generic, Java lambda/proxy, JS bundle hash ve release dizini normalizasyonu eklendi. | New Relic/Sentry benzeri iş akışı (çöz, yok say, ata, geri dönüşte yeniden aç) hata gruplarının ClickHouse'ta yalnızca istatistik olmasıyla kurulamazdı; durum küçük, işlemsel ve denetimli olmalı. Zamanlanmış tespit yerine okuma/alarm anında tespit: ek lider işi ve Postgres ↔ ClickHouse tarama döngüsü yok, sonuç aynı. Sürüm karşılaştırması ilk görülme zamanıyla yapılır çünkü sürüm adları sıralanabilir değildir. Bedeli: parmak izi v2 bazı grupların kimliğini yükseltmede bir kez değiştirir (eski grup emekliye ayrılır); kimse bakmazsa ve alarm yoksa yeniden açılma bir sonraki okumaya kadar kaydedilmez. | Kabul (uygulandı) |
| D-069 | 2026-09-14 | **Log imleçli sayfalama ve dağıtım tespiti sorgu anında:** `GET /api/v1/logs` sırası (`timestamp`, satır içeriği hash'i) azalan; yanıt opak `next_cursor` (zaman damgası ns, satır anahtarı, o konumda dönen satır sayısı) içerir, aynı nanosaniyedeki hatta birebir aynı satırlar da tam bir kez döner; `to` ile sayfalama geriye uyumlu kalır. Dağıtımlar (`service.version` değişimi) ClickHouse `apm_service_versions_1m` MV'sinden (dakika başına sürüm başına span) sorgu anında `apm.DetectDeployments` ile çıkarılır (30 dk boşluk, 24 sa geriye bakış, geri dönüş/ilk sürüm işaretleri); öncesi/sonrası karşılaştırması aynı RED/Apdex tanımlarıyla. | Log tablosunda satır kimliği yok; `to` taşıyarak sayfalama aynı zaman damgasındaki satırları tekrarlıyor ya da atlıyordu. Hash + sayaç, şema değişikliği ve yeni kolon olmadan kesin sıralama sağlar. Dağıtım olayları için processor'da durum tutmak (önceki sürümü bilmek) yatay ölçeklenen, durumsuz processor kuralına (D-006) aykırı; dakika başına sürüm tablosu küçük ve eşik (boşluk) sonradan değiştirilebilir. Bedeli: satır içeriği aynı ve aynı konumdaki çok sayıda satırda sayaç büyür; dağıtım listesi her istekte hesaplanır. | Kabul (uygulandı) |
| D-063 | 2026-09-14 | **OQL sorgu dili:** NRQL benzeri `SELECT agg… FROM Log\|Span\|Transaction\|Metric\|Host\|Container [WHERE] [FACET] [SINCE/UNTIL] [TIMESERIES] [LIMIT] [COMPARE WITH]` (`docs/contracts/oql.md`). El yazımı lexer/recursive-descent parser → AST → olay tipi başına beyaz listeyle doğrulanan plan → `internal/api/query` ile parametreli ClickHouse SQL; sorgu metninden SQL'e hiçbir metin kopyalanmaz (tanımlayıcılar sabit tablolardan, tüm değerler/map anahtarları/değişkenler `{pN:Type}` parametresi), tenant koşulu sorgu katmanında zorunlu. Sonuç tipleri single/facets/timeseries/histogram; `Metric` > 6 saatlik ve uyumlu sorgularda otomatik `metrics_1m`. Sınırlar: 8 KiB, ≤5 facet, ≤500 seri, ≤1000 bucket, ham aralık ≤31 gün (rollup 400), okuma kullanıcısı + organizasyon limitleri (D-047). | Kullanıcı sorgusunu SQL'e çevirmek en riskli yüzey: beyaz liste + parametre bağlama + fuzz/güvenlik testleri tenant kaçışını ve enjeksiyonu yapısal olarak imkânsız kılar; hazır SQL/PromQL gömmek tenant sınırını ve maliyet kontrolünü zorlaştırırdı. | Kabul |
| D-064 | 2026-09-14 | **Özel dashboard'lar PostgreSQL'de:** `dashboards` + `dashboard_pages` + `dashboard_widgets` (0020); belge (sayfalar, 12 kolonluk ızgarada widget'lar, OQL sorgusu, görselleştirme, eşikler, değişkenler) her kayıtta bütün olarak değiştirilir, `version` ile iyimser eşzamanlılık (409). Okuma: tüm üyeler ve API anahtarları; `private` yalnız oluşturana (kullanıcı silinince admin'e); yazma: üye+ kendi dashboard'unu, admin/owner herkesinkini. Değişkenler (`{{host}}`) sorgu metnine yerleştirilmez, bağlanan değer olur; değişkeni boş/`*` olan koşul `true`. İçe/dışa aktarma JSON (`openlog_dashboard: 1`), kopyalama, audit `dashboard.*`. | Yerleşim/izin/audit PostgreSQL'deki diğer yönetim verisiyle tutarlı; tüm-belge kaydı ızgara düzenlemesini basit tutar, sürüm kontrolü eşzamanlı düzenlemede sessiz kaybı önler. | Kabul |
| D-065 | 2026-09-14 | **`oql` alarm kuralı tipi:** koşul tek sayısal kolonlu OQL sorgusu (FACET → seri başına etiket; TIMESERIES/SINCE/UNTIL/COMPARE/histogram/değişken yok) + `window_seconds` + eşik. Değerlendirme aynı çevirmenle `[end−window, end)` üzerinde (ham veri); önizleme her olayı kapsadığı pencerelere `arrayJoin` ile dağıtan tek sorguyla tam sonuç verir (pencere/adım ≤ 720). | Kullanıcı panoda gördüğü sorguyu aynen alarma çevirebilir; ayrı bir koşul dili ve ikinci bir çevirmen bakım yükü olurdu. | Kabul |
| D-066 | 2026-09-14 | **Katmanlı saklama (hot → warm → S3):** ClickHouse storage policy `openlog_tiered`: volume `default` (disk `default`, tüm insert'ler), isteğe bağlı `warm` (ikinci yerel disk), `cold` = `object_storage`/`s3` disk (metadata yerel diskte) + sınırlı filesystem cache. Taşıma yalnızca TTL ile, tablo sınıfı başına ayrı gün (`OPENLOG_STORAGE_{COLD,WARM}_AFTER_DAYS_{METRICS,METRICS_1M,LOGS,TRACES,APM,ALERTS}`), silme süresinden bağımsız; küçük varlık tabloları yerel kalır. Zero-copy replikasyon kapalı: her replika kendi kopyasını `{shard}/{replica}` önekine yükler. Volume'larda `perform_ttl_move_on_insert=false` (insert S3'e hiç yazmaz), disk `skip_access_check=true` (S3 kapalıyken sunucu açılır). Yapılandırma sunucu tarafında `from_env` ile (Compose `storage-tiered.xml`, MinIO profili `tiered`; Helm operator modunda CHI + Secret/IRSA); `openlog-admin storage status` volume/tablo bazında bayt, bekleyen/başarısız taşıma ve detached part gösterir. | Sıcak disk maliyeti retention'ı sınırlıyordu; sorgu yolu ve şema değişmeden eski veri ucuz depoya gider. `s3_plain_rewritable` metadata'yı bucket'ta tutar ve MergeTree'nin hard link gerektiren işlemlerini (mutation, move) tam desteklemez; zero-copy upstream'de production-ready değil (merge/replika kaybında veri kaybı ve sahipsiz nesne hataları) — bedeli S3 baytının replika sayısıyla çarpılması. Canlı testte (2×2 + MinIO) S3 kesintisinde insert ve sıcak sorgular etkilenmedi, soğuk okumalar `S3_ERROR` ile düştü, kesinti sonrası veri tam döndü. Hot volume adı `default` olmak zorunda: ClickHouse eski politikanın volume'unu içermeyen politikaya geçişi reddediyor. | Kabul (uygulandı; Helm operator modu ve SSE gerçek ortamda denenmedi) |
| D-067 | 2026-09-14 | **Telemetri tablolarının TTL'inin tamamı openlog-migrate'e ait:** migration'lardan sonra tek adım (`ApplyTableTTLs`) yönetilen tabloların TTL ifadesini retention (şema değeri; APM için `OPENLOG_APM_RETENTION_DAYS`) + taşıma cümlelerinden üretir; önce gerekiyorsa her replikada politikanın varlığını denetleyip `MODIFY SETTING storage_policy, materialize_ttl_recalculate_only = 1`, sonra `MODIFY TTL` (hepsi `ON CLUSTER`) çalıştırır ve uygulanan ifadeyi `table_settings`'e `ttl:<tablo>` olarak yazar (kayıtlar tüm replikalardan okunur). Kayıt yoksa şema TTL'i varsayılır, böylece tiering hiç açılmamışsa hiçbir şey değişmez; eski `apm_retention_days` kaydı okunmaya ve yazılmaya devam eder. Tiering kapatılınca taşıma cümleleri kalkar, politika kalır. `openlog-migrate -plan` bekleyen ALTER'ları gösterir. | APM retention ve taşıma aynı `MODIFY TTL`'i ayrı ayrı yazsaydı biri diğerini silerdi. Kayıt karşılaştırması `engine_full` metnini ayrıştırmaktan kararlı; recalculate-only, büyük kurulumlarda tiering açılınca tüm part'ların yeniden yazılmasını önler (süresi geçen satırlar sonraki TTL merge'ünde silinir). `default` politikasına geri dönüş ClickHouse'ta mümkün değil. | Kabul (uygulandı) |
| D-070 | 2026-09-14 | **Kubernetes: aynı infra agent ikili dosyası, iki mod, client-go yok:** `deploy/helm/openlog-agent` (Apache-2.0) node modunda DaemonSet (host kökü salt okunur, `hostPID`/`hostNetwork`, root + yalnızca `DAC_READ_SEARCH`/`SYS_PTRACE`), cluster modunda Deployment çalıştırır; mod `kubernetes.enabled: auto` (`KUBERNETES_SERVICE_HOST`) + `kubernetes.mode`. Node agent kendi node'unun pod'larını izler (`fieldSelector=spec.nodeName`, list + watch, 5 dk'da bir ve `410 Gone`'da yeniden list) ve container metrik/log kaynaklarına `k8s.pod.uid`, `k8s.node.name`, `k8s.cluster.name`, sahip (Deployment = ReplicaSet adı − `pod-template-hash`, CronJob = Job adı − `-<rakamlar>`), izinli etiketler (`k8s.pod.label.*`) ekler; kubelet `/stats/summary` metrikleri `k8s.node/pod/container.*` adlarıyla (container için `container.*` yerine `k8s.container.*`: cgroup metrikleriyle çakışmasın). Nesne kimliği resource değil data point/log attribute'u (container metrikleri gibi): seriler node'un shard'ında kalır, alarmlar `attr.*` ile süzer. Küme kimliği `kube-system` namespace uid'i (`k8s.cluster.uid`). | Tipli küçük REST istemcisi (list, watch, Lease) client-go'nun bağımlılık ağacını ve ikili boyutunu her node'a taşımaz; agent yalnızca birkaç salt okunur GET, iki watch ve bir Lease kullanır. Node modunda ReplicaSet/Job sorgusu yerine ad heuristiği API sunucusuna node sayısıyla orantılı yük bindirmez; cluster modu kesin sahipliği zaten listelediği ReplicaSet/Job'lardan çözer. `privileged` yerine dar yetenek seti; host kökünü okuyabilmek node'daki Secret hacimlerini okuyabilmek demek (belgelendi). Bedeli: `ReplicaSet` adı kuralına uymayan (elle oluşturulmuş) sahipler node modunda Deployment'a çözülmez; kubelet sertifikası varsayılan olarak doğrulanmaz (`node.kubelet.insecureSkipVerify`). | Kabul (uygulandı) |
| D-071 | 2026-09-14 | **Cluster collector: Lease ile tek lider, watch cache'ten periyodik list, varlıklar MV'lerle:** cluster modunda `coordination.k8s.io/v1` Lease (15 sn süre, 5 sn yenileme, kapanışta bırakma) tutan tek replika her `kubernetes.cluster.interval` (30 sn) `resourceVersion=0` ile node, pod, workload, job/cronjob, HPA, namespace, quota listeler ve OTel `k8s_cluster` adlarıyla metrik + `openlog.k8s.{cluster,node,pod,workload}.status` bilgi noktaları (değer 1, durum attribute'ları) gönderir; event'ler tek watch ile log kaydı (`event_name = k8s.event`, uid + count ile tekilleştirme, başlangıçta 5 dk'dan eski event'ler atlanır). Cluster resource'unda `host.id` yok (`openlog.entity.type = k8s_cluster`; host satırı yazılmaz). Backend'de `k8s_clusters/nodes/workloads/pods` tabloları `metrics_local` üzerindeki MV'lerle (0040–0044, `containers_mv` gibi argMax durumları) tutulur; API her okumada GROUP BY ile yeniden birleştirir. | Informer (sürekli watch + bellek içi kopya) yerine watch cache'ten list: agent durumsuz kalır, lider değişiminde ısınma yok, API sunucusu etcd'ye gitmez; 30 sn aralık kube-state için yeterli. Bilgi noktaları + MV, processor'a Kubernetes'e özel kod eklemeden varlık tablosu verir (D-043 container deseni) ve alarm kuralları aynı metrikleri kullanır. Bedeli: çok büyük kümelerde (on binlerce pod) her aralıkta tam liste (pod başına ~15 seri; `kubernetes.cluster.max_pods` sınırı); tüm cluster serileri tenant başına tek shard'a düşer (`host_id = ''`); lider değişiminde son 5 dk'nın event'leri yeniden gönderilebilir (at-least-once). | Kabul (uygulandı) |
| D-072 | 2026-09-14 | **Java agent: upstream OpenTelemetry Java agent + openlog extension, tek jar.** `openlog-javaagent-<sürüm>.jar` (`agents/java`, Apache-2.0) değiştirilmemiş upstream agent'tır (2.31.1, `gradle.properties`'te sabit); openlog extension'ı jar içindeki `extensions/` dizinine gömülür (fork, yeniden derleme, shading yok; SPI: `AutoConfigurationCustomizerProvider`, `ConfigurableSamplerProvider`). `OPENLOG_*` değişkenleri (ve `openlog.*` sistem özellikleri) `otel.*` özelliklerine eşlenir; varsayılanlar: http/protobuf + gzip, traces/metrics/logs OTLP. Örnekleyici Go `sampler.go`'nun portudur ve Go fikstürleriyle doğrulanır; W3C random bayrağını OTel Java SDK kendisi koyar. DB metni span exporter zincirinde Go algoritmasıyla temizlenir; upstream sanitizer varsayılan kapalıdır (MongoDB/Elasticsearch/OpenSearch upstream'de kalır). Kaynak: infra agent `host.id` zinciri ve `container.id` eklenir, `process.command_line/args` çıkarılır, `telemetry.distro.name=openlog`. Dağıtım: GitHub release'e jar + `.sha256` (`release.yml` `java-agent-jar`); imzalı manifest ve Maven Central sonraki iş. CI `java-agent` job'u JDK 17/21'de birim ve entegrasyon testlerini çalıştırır (Spring Boot 3 MVC/WebFlux, PostgreSQL/MySQL/Hibernate, Lettuce/Jedis, Kafka, gRPC, Logback/Log4j2, OTLP yakalama). | Java'da OTel agent'ı fiili standarttır ve 100+ kütüphaneyi kapsar. Kendi bytecode instrumentation'ımızı yazmak yerine openlog'a özgü sözleşme (host bağlantısı, örnekleme ağırlığı, DB temizleme, lisans başlığı) SPI ile eklenir; upstream yükseltmesi sürüm değişikliğinden ibarettir. Exporter katmanında temizleme, ham metni ihraç etmeden Go ile birebir sonuç verir ve `off` modunu (öznitelik silme) mümkün kılar; bedeli ham ifadenin ihraç kuyruğunda bellekte kalmasıdır. Extension Java 8 bytecode'udur, çünkü agent Java 8 uygulamalarını destekler. | Kabul (uygulandı) |
| D-073 | 2026-09-14 | **Python agent: OpenTelemetry Python SDK + contrib instrumentation'ları üzerine openlog dağıtımı `openlog-agent` (PyPI, import `openlog_agent`, `agents/python`, Apache-2.0, Python 3.9–3.13).** Kodsuz başlatma `openlog-instrument <komut>` (`opentelemetry-instrument` sarmalayıcısı; `opentelemetry_distro` ve `opentelemetry_configurator` giriş noktaları `openlog`) veya `openlog_agent.start()`; ortam değişkenleri Go/Node agent'larla aynı (`OPENLOG_*` > `OTEL_*`). OTel paketleri tam sürümle sabitlenir: Python ≥ 3.10 için SDK 1.44.0 / contrib 0.65b0, Python 3.9 için onu destekleyen son hat 1.41.1 / 0.62b1 (ortam işaretleriyle tek pakette). Varsayılan `OTEL_SEMCONV_STABILITY_OPT_IN=http,database`. Django, Flask, FastAPI/Starlette, requests/httpx/aiohttp/urllib/urllib3, psycopg2/psycopg/asyncpg, PyMySQL/mysqlclient, SQLAlchemy, redis, Celery, gRPC; stdlib logging OTLP log + kayıtlara `trace_id`/`span_id` (structlog işlemcisi). Örnekleyici Go fikstürleriyle doğrulanan port; SDK örnekleyicileri span başlangıç özniteliklerini sonuçta döndürmek zorunda (döndürmeyen örnekleyici gRPC `rpc.*` özniteliklerini siliyordu, testle korunur). DB metni ve Django `http.route` (baştaki `/`) dışa aktarma yolunda, Flask `http.route` istek kancasıyla düzeltilir. Fork: SDK export thread'lerini çocukta yeniden başlatır, agent tüm provider'ların kaynağında `process.pid`'i günceller (gunicorn `--preload` dahil, Celery prefork); kod başlatma için gunicorn `post_fork` kancası; SIGTERM varsayılan eylemdeyken flush sonrası sinyal yeniden gönderilir. Runtime metrikleri `cpython.gc.*`, `openlog.cpython.gc.{time,pause.max}`, `process.*`. PyPI yayını tag'den trusted publishing (OIDC, `pypi` environment, `PYPI_PUBLISH` değişkeni). | Python'da OTel standart yol; openlog'a özgü kısım (host.id zinciri, örnekleme, DB temizleme, route adlandırma, fork) diğer agent'larla aynı sözleşmeyi uygular. `openlog` adı PyPI'da ilgisiz bir projeye ait. OTel 1.42 Python 3.9'u bıraktı; 3.9 kullanıcıları için ikinci sabit hat bakım yükü ve o hat yeni upstream düzeltmelerini almaz. GC süresi histogram yerine sayaç + gauge: GC geri çağrısından SDK enstrümanına yazmak SDK kilidine yeniden girip kilitlenebilir. Trusted publishing uzun ömürlü token gerektirmez. Bedeli: upstream ASGI middleware örneklenmemiş isteklerde de maliyetli (FastAPI ~0,13 ms/istek, örneklenmiş ~0,39 ms). | Kabul (uygulandı) |
| D-074 | 2026-09-14 | **.NET agent: OpenTelemetry .NET üzerine openlog dağıtımı `OpenLog.Agent` (NuGet, `agents/dotnet`, Apache-2.0).** `net8.0` (ASP.NET Core, generic host) + `netstandard2.0` (.NET Framework 4.6.2+ ve diğer runtime'lar, `OpenLogAgent.Start`); OpenTelemetry .NET 1.18.0 tam sürümle sabitlenir (merkezi `Directory.Packages.props`). Kodla: `builder.Services.AddOpenLog()` veya `OpenLogAgent.Start()`; kodsuz: OTel .NET automatic instrumentation + `OTEL_DOTNET_AUTO_PLUGINS` ile pakette gelen openlog eklentisi (önizleme). Ortam değişkenleri Go/Node agent'larıyla aynı (`OPENLOG_*` > `OTEL_*`); host.id zinciri, container.id, Go örnekleyicisinin portu (Go fikstürleriyle doğrulanır), Go ile aynı SQL temizleme (paylaşılan test vakaları), lisans başlığı, gzip. `ActivityTraceFlags` yalnızca sampled bitini modellediği için W3C random bayrağı her yeni Activity'ye bir ActivityListener ile eklenir; SDK ve runtime (`DistributedContextPropagator`) için bayrağı koruyan kendi W3C propagator'ı kullanılır. ASP.NET Core minimal API/MVC/gRPC, HttpClient, SqlClient, Npgsql/MySqlConnector/MassTransit'in yerleşik ActivitySource'ları, EF Core (sağlayıcı span'leri), StackExchange.Redis (uygulama OTel paketini referans ediyorsa otomatik), ILogger → OTLP log, `dotnet.*` runtime + `process.*` metrikleri. NuGet yayını ürün tag'inden: `release.yml` `dotnet-agent-nuget` (`NUGET_API_KEY` yoksa uyarıyla atlanır, `--skip-duplicate`); CI `dotnet-agent` .NET 8/9'da birim + gerçek PostgreSQL/MySQL/Redis ile entegrasyon testleri. | .NET'te OTel standart yol; önsürüm instrumentation paketleri (Redis, EF Core) pakete bağımlılık olarak eklenmez, tek paket yeterli. Bilinen fark: OTel .NET örneklenmeyen yerel ebeveynin altında Activity oluşturmaz, bu yüzden aşağı yöne ebeveynin span id'si yayılır (karar ve tracestate Go/Node ile aynı). Giriş noktası `OpenLog.Start()` değil `OpenLogAgent.Start()`: `OpenLog` adlı statik sınıf `OpenLog.Agent` ad alanını gölgelerdi. | Kabul (uygulandı) |
| D-075 | 2026-09-14 | **Tail-based sampling ayrı aşama olarak:** `openlog-sampler` (consumer group `openlog-sampler`) ham `<prefix>.otlp.traces.v1` topic'ini okur, span'leri (tenant, trace id) başına tamponlar, ilk span'den `OPENLOG_TAILSAMPLING_DECISION_WAIT` (30 s) sonra tenant politikasıyla karar verir ve tutulanları `<prefix>.otlp.traces.sampled.v1`'e yazar; processor bu durumda ham yerine sampled topic'i okur. Açıkken ingest trace isteklerini trace id'ye göre böler (anahtar `<tenant>/<trace id>`), böylece bir trace tek partition'a ve tek sampler'a düşer. Politika organizasyon başına PostgreSQL `tail_sampling_policies` (0040) JSON'u: sıralı kurallar (`error`, `latency`, `service`, `route`, `attribute`, her biri `ratio`), ilk eşleşen kazanır, yoksa `baseline_ratio`; tenant başına span/s sınırı olasılığa katlanır. Bellek sınırları (izli trace, trace başına span, bayt) doldurunca en eski trace hemen karara bağlanır (hiç atılmaz). Tutulan span'lerin tracestate'i `ot=th:T(head × tail)` olarak yeniden yazılır; karar trace randomness'ına göre OTel tutarlı olasılık örneklemesiyle (`R ≥ T(head·q)`, head eşiği yoksa trace id hash'i) verilir, böylece processor'ün mevcut `sample_weight = 1/p` hesabı değişmeden RED/Apdex tahminleri yansız kalır (özellik testi: 200k trace, sapma < %2,5). Geç gelen span'ler karar önbelleğini (10 dk) izler. Rebalance'ta bırakılan partition'ların trace'leri revoke callback'inde karara bağlanıp üretilir ve commit edilir; kapanışta hepsi. Kapalıyken (varsayılan) davranış değişikliği yok; sampled topic yine de migrate ile oluşturulur. | Head sampling hataları ve yavaş istekleri korumaz; agent'larda tail sampling her dilde ayrı ve trace'in tamamını göremez. Processor'e gömmek durumsuz processor kuralını (D-006) ve chunk/dedup token determinizmini bozardı; ayrı consumer group bağımsız ölçeklenir ve kapalıyken sıfır maliyetlidir. Ağırlığı yeni kolon yerine `ot=th` ile taşımak processor/şema/APM MV'lerini değiştirmez ve agent head sampling semantiğiyle birleşir. Bedeli: trace başına Kafka kaydı (ingest'te ek kayıt ve partition anahtarı değişikliği), karar gecikmesi kadar görünürlük gecikmesi, rebalance/uzun trace'lerde bölünmüş kararlar, oran sınırı örnek başına (N kopyada ≈ N × sınır). | Kabul (uygulandı) |
| D-076 | 2026-09-14 | **Tail sampling teslim semantiği: at-least-once, Kafka transaction yok.** Sampler tutulan span'leri (acks=all, idempotent producer) üretir, onay sonrası ham topic offset'lerini partition başına hâlâ tamponda span'i ya da üretilmemiş çıktısı olan en eski kayda kadar commit eder. Çökme, başarısız commit, kaybedilen (lost) partition veya kısmi üretim hatasında yeniden deneme tutulan span'leri ClickHouse'ta çoğaltabilir; processor dedup token'ları sampled topic offset'lerine bağlı olduğundan bunları elemez. Olasılıksal kararlar trace randomness'ıyla deterministik olduğundan tekrar okuma aynı trace'i yine tutar (düşürülen trace tekrar tutulmaz); oran sınırlı kararlar hariç. | Kafka transaction'ları (transactional sampler + `read_committed` processor) çoğalmayı kaldırırdı ama: transaction'lar karar gecikmesi boyunca açık kalamaz (her flush'ta commit gerekir), asılı bir sampler LSO'yu durdurup processor'ü tüm sampled topic'te bekletir, dinamik grup üyeliğinde transactional.id çitlemesi ve revoke'ta abort ek karmaşıklık getirir, throughput düşer. Pipeline'ın geri kalanı zaten at-least-once (D-018, kafka.md); çoğalma penceresi yalnızca arıza anlarıyla sınırlı. Gerekirse sonraki adım: sampled kayıtlara kaynak offset aralığından türetilen deterministik anahtar ile processor tarafında dedup. | Kabul (uygulandı) |
| D-079 | 2026-09-14 | **Kullanım ölçümü saklanan satırlardan türetilir:** ClickHouse `0050_usage`: `usage_signals_1h` (tenant/saat/sinyal/servis/host başına öğe sayısı ve `byteSize` ile tahmini sıkıştırılmamış bayt; spans/logs/metrics `_local` tabloları üzerinde MV), `usage_entities_1d` (gün başına tekil host/container/servis), `usage_ingest_1h` (processor'ün her Kafka kaydının protobuf baytını alındığı saate yazması; chunk'ın dedup token'ıyla), `usage_queries_1h` (api lideri `system.query_log`'u `log_comment.tenant_id` ile 15 dk'da bir son 3 saat için yeniden toplar; ReplacingMergeTree). Hepsi 400 gün. Aktif host = son gün ile önceki günün tekil host sayısının büyüğü; kullanıcı = PostgreSQL üyeleri. Fatura dönemi UTC takvim ayı; ay sonu tahmini son 7 tam günün ortalamasıyla. | Ingest sayaçları yeniden deneme ve Kafka tekrar teslimde çift sayar, faturalanan ise tenant'ın sorgulayabildiği veri olmalı. Mevcut dedup token + `deduplicate_blocks_in_dependent_materialized_views` MV'leri de idempotent yapar (entegrasyon testi: aynı token'larla tekrar yazımda ham satır sayısıyla birebir). Bedeli: zamanlamaya bağlı chunk kesimlerinde telemetri gibi kullanım da çift yazılabilir; bayt tahmini (sıkıştırılmış değil) ve tail sampling açıkken örneklenmiş trace baytı. | Kabul (uygulandı) |
| D-080 | 2026-09-14 | **Planlar yapılandırmadır, zorlama PostgreSQL üzerinden koordine edilir:** plan kataloğu JSON (`OPENLOG_PLANS`/`_FILE`; aylık GiB, host, kullanıcı, sinyal başına saklama, sorgu limitleri, ingest bayt/s + burst, `hard_ingest_limit` + `grace_percent`, sağlayıcıya özgü `billing.plan_ref`), organizasyon ataması ve kısmi override'lar `org_plans` (0035). api lideri dakikada bir tüm organizasyonları değerlendirip `tenant_quota_status`'a yazar; %80/%100 eşiklerinde sahiplere dönem başına bir kez e-posta (`usage_notifications` claim), UI banner. Yalnızca `OPENLOG_SAAS_MODE=true`'da ingest: aylık kota + grace aşılınca `429`/`RESOURCE_EXHAUSTED` + `Retry-After` (D-014'teki tenant limit durumu), tenant başına pod'da token bucket (hız/pod sayısı; pod sayısı `component_heartbeats`'ten, borçlanabilen kova). PostgreSQL 15 dk'dan uzun erişilemezse ingest serbest bırakır (fail open). Plan sorgu limitleri api'de `OPENLOG_QUERY_*` varsayılanlarının üzerine uygulanır, `OPENLOG_QUERY_TENANT_LIMITS` önceliklidir. Süper yönetici `OPENLOG_SUPERADMIN_EMAILS` (doğrulanmış e-postalı oturum). Self-hosted varsayılan: tek `unlimited` plan, zorlama yok. | Fiyatlar faturalama sağlayıcısında değişir; kodda plan tutmak her fiyat değişikliğinde sürüm gerektirirdi. Redis gibi yeni bağımlılık yerine mevcut PostgreSQL ve heartbeat tablosu yeterli: kota kararı dakikalık hassasiyetle, hız sınırı pod başına yaklaşık (dengesiz yük dağılımında sınırın altında kalabilir; `OPENLOG_QUOTA_INGEST_PODS` ile sabitlenebilir). Faturalama sağlayıcısı henüz seçilmedi: `internal/billing` arayüzü (müşteri, abonelik, idempotent kullanım gönderimi, webhook doğrulama), `noop`, günlük gönderim işi (`billing_usage_pushes`, anahtar `openlog:<org>:<gün>:<metrik>`) ve abonelik webhook'unun plana eşlenmesi. | Kabul (uygulandı; gerçek sağlayıcı yok) |
| D-081 | 2026-09-14 | **Tenant başına saklama: tablo TTL'i en uzun plan saklaması + günlük partition kapsamlı silme.** `OPENLOG_QUOTA_RETENTION_ENABLED` (SaaS'ta varsayılan) ile openlog-migrate logs/traces/metrics ham tablolarının silme TTL'ini planların en uzun etkin saklamasına çıkarır (saklama belirtmeyen plan şema varsayılanını sayar; D-067 TTL sahipliği üzerinden). api lideri saatte bir, saklaması daha kısa tenant'ları gün sayısına göre gruplayıp süresi dolmuş gün partition'ları için `ALTER TABLE <t>_local ON CLUSTER DELETE IN PARTITION ID '<gün>' WHERE tenant_id IN (…)` gönderir (satır varsa; aynı tenant kümesi için 24 saatte bir, çalışma başına sınırlı, `usage_retention_mutations`). Override'lar en uzun plan saklamasını aşamaz. Agregalar (metrics_1m, APM) global saklamada kalır. | (a) Partition anahtarına saklama katmanı eklemek tüm ham tabloların yeniden oluşturulması demek ve plan değişikliği yalnız yeni veriye uygulanır. (b) Satıra yazılan saklama sütunuyla TTL, saklamayı insert anında dondurur ve karışık tenant'lı günlük partition'larda `ttl_only_drop_parts` kullanılamadığından part'ları yeniden yazan TTL merge'leri gerektirir. Seçilen yol şema değiştirmez, plan değişikliğine geriye dönük uyar ve kararlı durumda tablo × farklı saklama başına günde bir partition yeniden yazar. Bedeli: disk en uzun saklamaya göre planlanmalı, downgrade birkaç çalışmada yetişir, çok geç gelen eski zaman damgalı satırlar tablo TTL'ine kadar kalır. | Kabul (uygulandı) |
| D-082 | 2026-09-14 | **PHP agent'ın filo kurulumu infra agent'ın mevcut ayrıcalıklı ön başlatma adımında (`-apply`) yapılır:** agent (yetkisiz) sürümü doğrular, `state_dir/php-agent`'a indirir, istek yazar ve bir kez çıkar; bir sonraki başlangıçta root `-apply` her şeyi yeniden doğrular, `/opt/openlog/php-agent/versions/<v>`'ye root sahipli kurar, `current`'ı değiştirir, `openlog-php-install install [--reload]` çalıştırır, modül yüklenmezse önceki sürüme döner (yoksa kaldırır) ve `php-agent-status.json` yazar. Sağlık kontrolü (varsayılan 5 dk) agent'ta; başarısızlıkta aynı yolla geri alma isteği. Paket/elle kurulumlar (işaret dosyası yok) hiç değişmez. | Ayrı root oneshot + path unit, agent kullanıcısının istediği an tetikleyebildiği ikinci bir root giriş noktası ve paketlerin/`install.sh`'nin kurup taşıması gereken yeni unit'ler demekti; `-apply` zaten güvenilir yapılandırma, derlenmiş anahtarlar ve `state_dir`'i düşman girdi sayan okuma kurallarıyla çalışıyor. Bedeli: her PHP agent değişikliğinde agent bir kez yeniden başlar (birkaç saniyelik telemetri boşluğu, o anda gelen PHP span'leri kaybolur); agent kendi güncellemesi sürerken PHP isteği göndermez. | Kabul (uygulandı) |
| D-083 | 2026-09-14 | **PHP agent filo politikası güncelleme politikasının bir bölümüdür, ayrı rollout nesnesi yoktur:** `agent_update_policies.php_agent` (`mode` varsayılan `manual`, `version` varsayılan `agent` = sunucunun infra agent sürümü, `reload`, `exclude_bins`, `changed_at`), host başına mod geçersiz kılma (`agent_php_host_overrides`, `auto` dalgaları atlar), host envanteri `agent_hosts.php_agent` (migration 0045). Karar saf fonksiyon (`DecidePHP`): politikanın dalgaları ve bekleme süresi `changed_at` ile hedef sürümün yayın zamanının geç olanından itibaren, `fnv32a(host_id + ":php-agent:" + hedef)`, bakım pencereleri; geri alınmış sürüm tekrar sunulmaz. Sync yanıtı `php_agent` bölümü hedefi ve imzalı manifesti taşır. | Hedef sürüm varsayılan olarak infra agent sürümünü izlediği için infra agent rollout'ları zaten dalgalı; PHP için ikinci bir rollout denetleyicisi (durdurma oranı, sayaçlar) karmaşıklık getirirdi. Hata durumu hostta geri almayla sınırlanır ve Fleet sayfasında host bazında görünür. Bedeli: PHP agent kurulumları için otomatik durdurma yok. | Kabul (uygulandı) |
| D-084 | 2026-09-14 | **Tenant sorgu limitleri ayar başına dört katmandan çözülür:** `OPENLOG_QUERY_*` varsayılanları < plan `limits.query` (org_plans override'ı dahil, yalnız `> 0` değerler) < organizasyon ayarı (`org_query_limits`, migration 0051; NULL = devral, `0` = openlog ayarlamaz) < `OPENLOG_QUERY_TENANT_LIMITS` (üç ayarı birden değiştirir). Organizasyon katmanını süper yöneticiler her zaman, sahipler yalnız `OPENLOG_SAAS_MODE` kapalıyken değiştirir (audit `query_limits.update`/`.delete`). api ve alert pod'ları plan + organizasyon katmanlarını tüm tenant'lar için 30 sn'de bir yükler (`quota.QueryLimitsResolver`); değişikliği işleyen pod hemen yeniler, sorgu yolu PostgreSQL'e gitmez, yükleme hatasında son katmanlar kalır. | Limitler SaaS'ta satılan planın parçası (D-080), self-hosted'da ise operatör kendi organizasyonlarını env düzenleyip yeniden başlatmadan ayarlayabilmeli. Env override'ının en üstte kalması operatöre acil durumda son sözü verir. İstek başına sorgu yerine önbellek: limitler her ClickHouse sorgusunda okunur; bedeli diğer pod'larda en fazla 30 sn gecikme. Mevcut `org_plans.overrides.query` ayrı tablo yerine kullanılmadı çünkü o yalnız süper yöneticinin plan atamasıyla birlikte yazılır ve SaaS dışında sahiplere açılamaz. | Kabul (uygulandı) |
| D-085 | 2026-09-14 | **İşlem e-postalarının dili isteğin `Accept-Language`'ından seçilir ve oluşturulan satırla saklanır:** şablonlar `internal/mail/templates` (gömülü `text/<ad>.<dil>.tmpl` + `html/<ad>.<dil>.tmpl`, ortak HTML düzeni, `html/template` ile kaçışlı), diller `en`, `tr` (desteklenmeyen = İngilizce). Davet: davet edenin dili `invitations.locale`'de, yeniden gönderimde korunur; doğrulama: yeni kullanıcının dili (`email_verifications.locale`); SSO alan adı doğrulaması: isteyen yöneticinin dili; kullanım bildirimleri: her sahibin hesabı oluşturulurken kaydedilen `users.locale` (migration 0050). | Organizasyon veya kullanıcı dil ayarı henüz yok; web arayüzü dili tarayıcıdan seçtiği için aynı sinyal tutarlı. Davet edilen kişinin dili bilinmediğinden davet edenin dili en iyi tahmin. Bedeli: mevcut kullanıcıların `locale`'i boş (İngilizce) ve kullanıcı dil tercihi ayarı gelene kadar sonradan değişmez. | Kabul (uygulandı) |
| D-077 | 2026-09-14 | **Organizasyon başına tek oturum açma (OIDC veya SAML 2.0) doğrulanmış alan adına ve organizasyona bağlıdır:** organizasyon başına bir bağlantı; OIDC yetkilendirme kodu + PKCE S256 + state/nonce (`coreos/go-oidc` v3.21, asimetrik algoritma allowlist'i, JWKS önbelleği + bilinmeyen `kid`'de yenileme), SAML SP `crewjam/saml` v0.5.1 üzerine kendi ön kontrollerimiz (tek Response/tek Assertion, DTD yok, tek imza, SHA-1 reddi, AudienceRestriction ve bearer SubjectConfirmation zorunlu, assertion ID replay önbelleği PostgreSQL'de). Bir kimlik yalnızca e-posta alan adı o organizasyonca DNS TXT veya yönetici e-postasıyla doğrulanmışsa kabul edilir; bir alan adını tek organizasyon doğrulayabilir. Giriş durumu PostgreSQL'de (10 dk, tek kullanım), tarayıcıya SameSite=Lax bağlama çerezi ile bağlanır; SAML ACS doğrulanan kimliği saklayıp aynı çerezi taşıyan GET `/sso/saml/complete`'e 303 yapar. SSO oturumu (`sessions.auth_method/org_id/sso_connection_id`) yalnızca kendi organizasyonunda, bağlantı etkin ve aynıyken, en uzun oturum süresine kadar geçerlidir; bu ve zorunluluk `auth.SessionPolicy` ile her istekte PostgreSQL'den okunur. Zorunluluk yalnızca doğrulanmış alan adındaki üyelerin parola oturumlarını keser; break-glass sahipleri hariç; açmak için etkin + mevcut ayar sürümüyle başarılı test girişi + doğrulanmış alan adı + en az bir break-glass sahibi + çağıranın erişimini koruması gerekir. JIT ve grup → rol eşlemesi sahipleri asla değiştirmez. İstemci gizli anahtarı ve SP anahtarı `OPENLOG_SSO_SECRET_KEY` (yoksa `OPENLOG_KEY_HASH_SECRET`'tan türetilen anahtar) ile AES-GCM şifrelenir; IdP URL'leri SaaS'ta yalnız https + public adres (DNS sonrası dial kontrolü). | Alan adı doğrulaması olmadan bir organizasyon yöneticisi kendi IdP'sinde başka bir şirketin kullanıcısının e-postasını üretip hesabı ele geçirebilirdi; organizasyona bağlı oturum, ele geçirilen bir IdP'nin etkisini o organizasyonla sınırlar. Durumun sunucuda tutulması yatay ölçeklemeyi (D-006) ve tek kullanımlığı sağlar; Strict/Lax çerez kısıtları cross-site SAML POST'unda bağlama çerezini taşımadığı için iki adımlı tamamlama seçildi. Kilitlenme korumaları yanlış yapılandırılmış bir IdP'nin organizasyonu dışarıda bırakmasını önler. Politika her istekte okunur (üyelik gibi, D-046) ki kapatma/silme anında etkili olsun; bedeli oturum başına bir indeksli sorgu. Kütüphaneler: imza doğrulama elle yazılmadı; crewjam'ın gevşek varsayılanları (audience/bearer zorunlu değil, çoklu assertion, SHA-1) üst katmanda sıkılaştırıldı. | Kabul |
| D-078 | 2026-09-14 | **SCIM 2.0 provisioning organizasyon üyeliğini yönetir, sahiplere dokunmaz:** `/api/scim/v2` (Users, Groups, ServiceProviderConfig/ResourceTypes/Schemas; `eq` filtreleri, PATCH, sayfalama; bulk/sort/ETag yok), organizasyon başına `ols_` token'ları API anahtarı gibi hash'lenir (D-044). SCIM kullanıcısı = organizasyondaki üyelik (id = openlog kullanıcı id'si); e-posta doğrulanmış alan adında olmalıdır. `active=false`/DELETE üyeliği anında kaldırır, kullanıcının o organizasyona bağlı SSO oturumlarını ve orada oluşturduğu API anahtarlarını iptal eder, `scim_users.active=false` kalır ve JIT girişi kullanıcıyı geri eklemez; yeniden aktifleştirme üyeliği grup rolüyle geri verir. SCIM grupları rol eşlemeleriyle (SSO ile ortak tablo) rol belirler; eşleme yoksa roller değişmez. Sahipler SCIM ile devre dışı bırakılamaz/silinemez, rolleri değiştirilemez; e-posta değişikliği reddedilir. | IdP'ler (Okta, Entra ID) kullanıcı çıkışını SCIM ile bildirir; erişimin anında kesilmesi D-046 ile tutarlıdır. Sahiplerin korunması ele geçirilmiş/yanlış yapılandırılmış bir IdP'nin organizasyonu sahipsiz bırakmasını önler. Üyeliği SCIM kaydından ayırmak, IdP'nin yeniden aktifleştirmesini ve SSO ile SCIM'in aynı rol eşlemesini paylaşmasını sağlar. E-posta değişikliği hesabın kimliğini (ve diğer organizasyonlardaki üyeliklerini) değiştireceği için desteklenmez. | Kabul |
| D-086 | 2026-09-14 | **Dashboard sürüm geçmişi tam belge anlık görüntüsüdür, farklar okunurken hesaplanır:** her kayıt (oluşturma, `PUT`, widget ekleme, geri yükleme) belgenin tamamını (sayfa/widget id'leriyle) `dashboard_versions` (Postgres 0053) tablosuna aynı transaction içinde yazar; dashboard başına son 50 sürüm tutulur (kayıtta budanır), 0053 mevcut dashboard'ların güncel sürümünü geri doldurur. Farklar (ad/açıklama/görünürlük/değişkenler, sayfa ve widget ekleme/silme/değişen alanlar) id eşleştirmesiyle API'de hesaplanır. Geri yükleme saklanan belgeyi yeni sürüm olarak kaydeder (`restored_from`, iyimser eşzamanlılık, güncel görünürlük korunur, saklanan id'ler korunur). Cross-widget filtreler kayda girmez: URL'deki `filters` her widget sorgusuna `POST /query` `filters` olarak gider ve plan seviyesinde, olay tipinde nitelik varsa `WHERE`'e parametreli olarak eklenir (oql.md §7). | Delta saklamak geri yüklemeyi ve budamayı karmaşıklaştırırdı; belgeler küçük (≤ 20 sayfa × 100 widget) ve 50 sürümle sınırlı. Filtrenin istemci tarafında sorgu metnine eklenmesi enjeksiyon ve olay tipi uyuşmazlığı riski taşırdı; plan seviyesinde bağlama değişkenlerle aynı güvenceyi verir. Bedeli: rolling upgrade sırasında eski api pod'larının kayıtları geçmişte yer almaz; geri yüklenen eski sorgu artık doğrulanmıyorsa `400`. | Kabul (uygulandı) |
| D-087 | 2026-09-14 | **Paylaşım bağlantıları organizasyon başına isteğe bağlı, süreli ve hash'li; zamanlanmış raporlar sunucu tarafı HTML tablo:** `dashboard_org_settings.shares_enabled` varsayılan kapalı (admin/owner açar); bağlantı `olds_` + 256 bit token, yalnızca SHA-256 hash'i saklanır, 5 dk–90 gün sonra biter, iptal edilebilir, oluşturan üyelikten çıkınca veya paylaşım kapatılınca çalışmaz. Açık uç noktalar (`/api/v1/public/dashboards/{token}` ve widget sonucu) istemciden sorgu/aralık/değişken almaz: saklanan widget sorgusu, bağlantının (göreli veya sabit) aralığı ve kilitli değişkenleriyle, sahibi organizasyonun tenant scope'u ve limitleriyle çalışır; yanıtta sorgu metni, kullanıcı/organizasyon bilgisi, tablo ve okuma istatistiği yoktur; IP/token/başarısız arama başına pod içi hız sınırı, `no-store` + kısıtlayıcı CSP, erişim saatte en fazla bir kez audit'lenir. Raporlar (günlük/haftalık, IANA saat dilimi, tr/en, alıcılar üye veya izinli alan adı; özel dashboard yalnızca oluşturana) api liderinde dakikalık işle gönderilir: dönem (yerel tarih) göndermeden önce `dashboard_report_runs`'ta claim edilir (dönem başına en fazla bir kez, 6 sa yakalama penceresi), widget değerleri basit HTML tablolarıdır; PNG grafik için ayrı, isteğe bağlı bir renderer servisi planlanır. | Paylaşım herkese açık veri sızıntısı riski taşıdığından organizasyon kararı ve süre zorunlu; token'ın başka API'lere kimlik olarak kullanılamaması ve sorgunun istemciden alınmaması saldırı yüzeyini tek dashboard'un saklanan widget'larıyla sınırlar. Headless tarayıcı api imajını büyütür ve güvenlik yüzeyi ekler. Bedeli: hız sınırı pod başına (küme geneli değil), gönderim sırasında çöken lider o dönemi yeniden göndermez, raporlarda grafik yok. | Kabul (uygulandı) |
| D-088 | 2026-09-14 | **SSO bağlantı yaşam döngüsü: organizasyon başına birden fazla bağlantı, tek oturum kapatma, şifreli assertion ve arka plan yenileme:** Organizasyon en fazla 10 OIDC/SAML bağlantısı tutar; en eski bağlantı varsayılandır. Doğrulanmış alan adı `connection_id` ile bir bağlantıya yönlendirilir (boş = varsayılan); bir bağlantı yalnızca kendisine yönlenen alan adlarının kimliklerini kabul eder, zorunlu SSO ve break-glass sahipleri bağlantı ayarıdır ve yalnız o bağlantının alan adlarındaki üyelere uygulanır; rol eşlemeleri bağlantıya özel olabilir (yoksa organizasyon geneli, SCIM her zaman organizasyon genelini kullanır). Her SSO oturumunun IdP oturumu (`sso_sessions`: NameID, SessionIndex, OIDC `sub`/`sid`, mühürlü ID token) kaydedilir; kaydedilemezse giriş başarısız olur. "Her yerden çıkış" önce openlog oturumlarını (aynı bağlantıdaki diğer oturumlar dahil) iptal eder, sonra SAML'de SP anahtarıyla imzalı LogoutRequest (HTTP-Redirect sorgu imzası veya HTTP-POST zarf imzası), OIDC'de `id_token_hint` ve sabit `post_logout_redirect_uri` ile end_session_endpoint'e gönderir; dönüş yolu `/login` veya bağlantının izin listesi. IdP kaynaklı LogoutRequest yalnızca IdP metadata sertifikasıyla imzalıysa (SHA-1 yok), issuer/Destination/zaman/replay kontrollerinden geçerse NameID (+SessionIndex) eşleşen oturumları bitirir ve imzalı LogoutResponse döner. Şifreli assertion: AES-GCM/AES-CBC içerik ve RSA-OAEP (xmlenc 1.1 MGF1 dahil) anahtar taşıma kabul edilir, RSA PKCS#1 v1.5 ve 3DES reddedilir; imzalı elemanda iç içe SAML mesajı/assertion bulunamaz. Lider iş OIDC discovery/JWKS'i 30 dakikada bir, SAML metadata'yı cacheDuration/validUntil'e göre (15 dk – 24 saat) yeniler; OIDC belgeleri tüm pod'larca bir saat kullanılır, entity ID değişmeyen SAML metadata'sı `config_version` artırmadan saklanır; başarısızlıklar 5 dk → 1 saat geri çekilir, sağlık (ok/warning/error/unknown) arayüzde ve metriklerde görünür. | Şirket birleşmeleri ve bağlı şirketler farklı IdP'ler kullanır; alan adı yönlendirmesi bir IdP'nin başka bir alan adının kullanıcılarını onaylamasını (hesap ele geçirme) engeller ve zorunluluğu doğru kitleye uygular. Tek oturum kapatma, IdP'de oturumu kapatılan veya işten ayrılan kullanıcının openlog'da açık kalmasını önler; openlog oturumlarının IdP'ye gitmeden kapatılması IdP arızasında da güvenli sonuç verir. İmzasız veya tekrar oynatılan çıkış istekleri kullanıcıları zorla çıkarmak için kullanılabileceğinden reddedilir. Keycloak, Entra ID gibi IdP'ler assertion'ı şifreler; crewjam/saml'in desteklemediği AES-256-GCM ve MGF1-SHA-256 eklenirken zayıf algoritmalar ön kontrolde kapatılır. Metadata/JWKS'in önceden yenilenmesi sertifika ve anahtar rotasyonunda giriş kesintisini ve istek yolundaki ağ gecikmesini azaltır; sürüm artırmamak zorunluluğun ve devam eden girişlerin bozulmasını önler, entity ID değişikliği ise yönetici onayı ister. | Kabul |
| D-089 | 2026-09-14 | **Sahiplenilmiş alan adı SSO'ya yönlendirilir, SCIM e-posta değişikliği kurallıdır:** Doğrulanmış alan adı etkin bir bağlantıya yönleniyorsa o adresle self-servis kayıt `409 failed_precondition` alır; o organizasyonun daveti parola ile kabul edilemez (SSO girişi bekleyen daveti davet edilen rolle kabul eder, JIT kapalı olsa da); başka organizasyonların davetleri bağlantının `allow_external_invitations` ayarı (varsayılan açık) kapalıysa aynı şekilde reddedilir. `POST /invitations/lookup` ve discover arayüzün "SSO ile devam et" göstermesini sağlar. SCIM PUT/PATCH (`emails`, `emails[primary eq true].value`, `emails[type eq "work"].value`, e-posta olan eski userName'in değişmesi) hesabın e-posta adresini değiştirir: yeni adres geçerli ve organizasyonun doğrulanmış alan adında olmalı, başka hesapta kullanılmamalı (`409 uniqueness`), mevcut adres de organizasyonun doğrulanmış alan adında olmalı ve kullanıcı sahip olmamalı; değişiklik denetim kaydına yazılır. D-078'deki "e-posta değişikliği reddedilir" kuralının yerini alır. | Kurumun sahiplendiği adreslerle parola hesapları açılması SSO zorunluluğunu ve IdP'deki erişim kapatmayı atlatır; yönlendirme kullanıcıya açık bir yol gösterir. Dış davetlere varsayılan izin geriye dönük uyumluluğu korur, isteyen kurum kapatır. IdP'lerdeki ad soyad/e-posta değişikliklerinin (evlilik, alan adı taşınması) deprovision olmadan uygulanması gerekir; hem eski hem yeni adresin kurumun doğrulanmış alan adında olması başka bir kurumun hesabının ele geçirilmesini, sahip istisnası ise organizasyon kurtarma yolunun IdP tarafından değiştirilmesini önler. | Kabul |
| D-093 | 2026-09-14 | **Operator modunda Keeper ve replikasyon TLS'i, operator modunda upgrade sırası:** `clickhouse.tls.server.secretName` verildiğinde varsayılan olarak (a) CHK `settings` ile Keeper `tcp_port_secure` (`server.keeper.securePort`, 9281), `raft_configuration/secure` ve openSSL server/client ayarları, sertifika aynı Secret'tan pod volume'u (`fsGroup` 101); CHI `zookeeper.nodes[]` güvenli portta `secure: "yes"`; (b) `interserver_https_port` (`server.interserverHTTPS.port`, 9010) ve `interserver_http_port` `remove="1"` ile kaldırılır. İkisi `enabled: false` ile kapatılabilir. Ek olarak CNPG `smartShutdownTimeout` varsayılanı 15 sn ve operator modunda migrate hook'unun `OPENLOG_STORAGE_TIERING_ENABLED`'ı canlı CHI'de `config.d/openlog-storage.xml` yoksa `false` (`lookup`). | kind doğrulaması (1 shard × 2 replika, Keeper × 3): ClickHouse Keeper'a `secure://keeper-openlog:9281` bağlandı, Raft "SSL enabled", replikalar part'ları HTTPS 9010'dan indirdi (Keeper'daki replika host kaydı `scheme: https`), strict doğrulamada sertifikasız istemci reddedildi. Aynı Secret: tek sertifika yönetimi. Pre-upgrade hook'lar CR değişikliğinden önce çalıştığı için TLS iki upgrade ile açılır (önce sunucu, sonra istemciler; aradaki sürede doğrudan shard insert'leri el sıkışmada kalır, veri Kafka'da bekler) ve tiering'i açan ilk upgrade taşımayı atlar. Bedeli: çalışan ensemble'da Keeper TLS'ini açmak Raft'ı karışık bırakır, operator pod'ları sırayla yenilerken ~6 dk quorum yoktu (Replicated insert ve ON CLUSTER DDL durur). `disableInsecure` + strict: operator 0.27.3 istemci sertifikası sunamaz; plaintext Keeper 2181 operator probe'u için açık kalır. | Kabul (uygulandı; kind'de doğrulandı, `disableInsecure` ve tiering uçtan uca denenmedi) |
| D-095 | 2026-09-14 | **Dil tercihi kullanıcıda ve organizasyonda, e-posta dili sıralı çözülür:** `users.locale` korunur, `users.locale_explicit` (0060) değerin kullanıcının seçimi mi yoksa hesap oluşturulurken gelen `Accept-Language` mı olduğunu ayırır; `organizations.locale` (`''`/`en`/`tr`) organizasyonun varsayılan e-posta dilidir. API: `PATCH /api/v1/auth/me {"language": "auto"\|"en"\|"tr"}` (Me'de `user.language`), `PATCH /api/v1/orgs/current {"language"}` (admin/owner). E-posta dili: alıcının tercihi (adres bir kullanıcıya aitse) → organizasyon varsayılanı (yalnız organizasyon e-postaları: davet, alan adı doğrulama, kullanım bildirimi) → saklanan istek dili → İngilizce; doğrulama e-postası organizasyon e-postası sayılmaz. Web arayüzü tercih varsa tarayıcı dili yerine onunla açılır (Ayarlar → Profil); tercih varken üst çubuktaki dil seçici tercihi de günceller, `auto` iken seçim yalnız tarayıcıda kalır. Audit: `user.language_change`, `org.language_change`. | Ayrı kolon `users.locale`'in 0050 anlamını (usage e-postaları, eski sürümler) bozmadan tercihi ekler; organizasyon varsayılanı davet edilen henüz kullanıcı olmayan kişiler için tek anlamlı kaynaktır. | Kabul |
| D-096 | 2026-09-14 | **Paylaşım bağlantısı hız sınırı küme geneli, rapor alıcıları üyelikle birlikte temizlenir:** `internal/ratelimit` kayan pencere (önceki dakika × kalan pay + bu dakika) sayaçlarını UNLOGGED `rate_limit_counters` (0061; anahtar = sha256(amaç, özne)'nin 16 baytı, dakika kovası) tablosunda tutar. Her api pod'u sınırdan uzak istekleri yerel karar verir ve sayılarını saniyede bir tek `INSERT … ON CONFLICT DO UPDATE SET n = n + excluded.n` ile yazar (aynı ifadede güncel ve önceki kovayı okur); yazılmamış payı kalan boşluğun 1/8'ine ulaşan anahtar için istek başına eşzamanlı yazıp taze sayıyla karar verir (8 pod'a kadar aşım yok). PostgreSQL erişilemezse 5 sn pod başına sayar (fail open); lider işi `rate-limit-prune` eski kovaları siler; static modda pod başına. Üyelikten çıkarma (API ve SCIM deaktivasyonu/silme) aynı transaction'da kullanıcının adresini organizasyonun `dashboard_reports.recipients` listelerinden `array_remove` ile çıkarır, boş kalan raporu kapatır (0062 boş listeye izin verir; API hâlâ 1–20 ister); audit `dashboard.report.recipient_remove`. | Pod başına sayaçlar pod sayısıyla çarpılan sınır veriyordu; her istekte yazma yerine boşluğa göre eşzamanlı yazma DB yükünü sınırlar ve sınıra yakınken kesinlik sağlar. Gönderimde atlamak listede eski adresleri bırakıyordu. | Kabul |
| D-097 | 2026-09-14 | **Rapor PNG grafikleri ayrı renderer imajında, rapora bağlı kısa ömürlü render token'ı ile:** `openlog-renderer` (`ghcr.io/onuragtas/openlog-renderer`, Alpine + Chromium, chromedp; ana imaj tarayıcısız kalır) yalnızca iç `POST /v1/render` API'si sunar (`OPENLOG_RENDERER_TOKEN` paylaşılan sır, isteğe bağlı TLS/mTLS); istek yalnız `/print/<segment>` yolu ve token taşır, köken renderer'ın kendi `OPENLOG_RENDERER_UI_ORIGIN` ayarıdır. Her render ayrı Chromium süreci + geçici profil, katı zaman aşımı, eşzamanlılık sınırı ve kuyruk zaman aşımı (429); sayfa istekleri DevTools Fetch ile yalnız UI kökenine izinli, DNS yalnız o host'a çözülür. api lideri `olrt_` + JSON claim + HMAC-SHA256 token imzalar (anahtar `OPENLOG_KEY_HASH_SECRET`, yoksa `OPENLOG_SECRETS_KEY`'den türetilir; renderer'a verilmez), ≤ 10 dk (5 dk verilir), organizasyon + tenant + dashboard + etkin rapor + dönem; yalnız `GET /api/v1/render/dashboard[/widgets/{id}/result]` kabul eder (paylaşım bağlantısı yanıtlarıyla aynı redaksiyon). Token URL fragment'ında taşınır, sayfa adres çubuğundan siler. Rapor e-postası widget başına CID satır içi PNG (≤ 1 MiB, e-posta başına ≤ 10 MiB) gösterir; renderer hatası/zaman aşımı, sorgu hatası veya limit aşımında o widget HTML tabloya döner, düz metin her zaman tablo. Compose `renderer` profili ve Helm (Deployment + ClusterIP + NetworkPolicy) Chromium'u `--no-sandbox` ile ama kilitli konteynerde çalıştırır (root değil, salt okunur kök FS, tüm yetenekler düşük, no-new-privileges, dış ağ yok). | Tarayıcı ~780 MB ve geniş saldırı yüzeyi: api imajından ayrı tutulmalı ve ağdan yalıtılmalı. Paylaşım bağlantısını yeniden kullanmak organizasyon ayarına ve kalıcı hash'li kayıtlara bağlanırdı; durumsuz HMAC token'ı tüm api pod'larında doğrulanır, migration gerektirmez ve renderer ele geçirilse de yeni token üretilemez (yalnız eldeki token'lar süresi dolana dek tekrar oynatılabilir). Sunucu tarafı grafik çizimi web arayüzüyle görsel farklılık yaratırdı; print görünümü aynı `QueryResult` bileşenini kullanır. Bedeli: sorgular iki kez çalışır (tablolar + tarayıcı), sayfa WebSocket/IP-literal bağlantıları Fetch ile kesilmez (ağ politikası şart), varsayılan seccomp profilleri Chromium sandbox'ına izin vermez. | Kabul (uygulandı; konteyner entegrasyon testiyle doğrulandı, Kubernetes'te NetworkPolicy ve mTLS canlı denenmedi) |
| D-098 | 2026-09-14 | **IdP'den başlayan oturum kapatma (OIDC back-channel/front-channel, SAML SOAP) ve yenilenen SAML metadata'sına güven:** Bağlantı başına (a) `POST /api/v1/sso/oidc/{id}/backchannel-logout`: `logout_token` sağlayıcının JWKS'iyle asimetrik imzalı olmalı, `iss` = issuer, `aud` ∋ client ID, `iat` 5 dk + saat kayması içinde, `exp` (varsa) geçmemiş, `events` içinde `http://schemas.openid.net/event/backchannel-logout`, `nonce` yok, `jti` replay önbelleğinde (`sso_saml_assertions`, `oidc-logout:<jti>`), `sub` ve/veya `sid`; `sid` ile o IdP oturumunun (ikisi varsa `sub` de eşleşen) oturumları, yalnız `sub` ile öznenin bağlantıdaki tüm oturumları biter → 200, reddedilen → 400 `invalid_request`; (b) `GET …/frontchannel-logout?iss&sid`: `iss` issuer'a eşit, `sid` zorunlu, yanıt boş HTML + `frame-ancestors <issuer origin>` (X-Frame-Options yok), `no-cache, no-store`; (c) `POST /api/v1/sso/saml/{id}/slo/soap`: SOAP 1.1 zarfında tek imzalı `LogoutRequest` (DTD/`mustUnderstand` yok), front-channel LogoutRequest ile aynı kontroller (`Destination` isteğe bağlı: SOAP veya SLO URL), SP anahtarıyla imzalı `LogoutResponse`, aksi ayrıntısız SOAP fault. Reddedilen istekler istemci adresi başına 10 dk'da 60 ile sınırlı ve `sso.logout_failed` olarak denetlenir; `sid` araması PostgreSQL 0063 kısmi indeksiyle. Metadata URL'li SAML bağlantısı kaydedilirken güven kararı zorunlu: sabitlenmiş metadata imzalama sertifikası (boş bırakılırsa imzalı metadata'nın imzalayanı sabitlenir) ya da açık `allow_unsigned_metadata`. Yenilemede sabitlenmiş sertifikayla kök elemanda tek, kökün `ID`'sine referans veren zarflanmış imza (SHA-256+) şart; doğrulanan metadata'daki sertifika/uç nokta değişikliği otomatik uygulanır. Başka sertifikayla imzalı metadata veya imzasız metadata'da değişen sertifika/uç nokta `config.saml.pending_metadata` olarak bekler (sağlıkta gerekçe) ve admin `POST /api/v1/sso/connections/{id}/metadata/accept {digest}` ile onaylar (metadata yeniden alınır, aynı değişikliği içermeli; yeni imzalayan sabitlenir); geçersiz/eksik imza hiçbir şeyi değiştirmez. | Tarayıcısız çıkış (yönetici çıkışı, IdP oturum sonu) ve üçüncü taraf iframe'lerde openlog çerezi gelmez (`SameSite=Strict`): oturumlar kayıtlı IdP oturumuyla (`sid`/NameID) bulunur, front-channel'da `sid` bu yüzden zorunlu. Metadata SAML güveninin köküdür; URL'nin yanıtını değiştirebilen biri (DNS, ara katman, barındırma) imzalama sertifikasını değiştirip girişleri ele geçirebilirdi, bu yüzden yenileme yalnız imzası doğrulanan veya güvenlik açısından değişmeyen metadata'yı kendiliğinden uygular. İlk kayıtta imzalayanı sabitlemek yöneticinin gördüğü parmak izine dayanır (kayıtta metadata'ya zaten güveniliyordu). Ayarlar `config` jsonb'de: migration gerekmez, eski api'ler alanları yok sayar. Bedeli: imzasız metadata'lı IdP'lerde (Keycloak, Okta) ve imzalayan sertifika dönen IdP'lerde (Entra ID) sertifika rotasyonu artık yönetici onayı ister; front-channel tarayıcılar üçüncü taraf iframe'lerini engellerse çalışmaz (back-channel önerilir); jti kaydından sonra iptal başarısız olursa IdP'nin tekrarı replay sayılır; SOAP'ta LogoutRequest zarf içindeyken inclusive c14n ile imzalanmışsa (SAML önerisi dışı) doğrulanamaz. | Kabul (uygulandı; birim testleri, PostgreSQL entegrasyon ve Keycloak 26 uçtan uca: yönetici çıkışıyla OIDC back-channel ve SAML SOAP, Keycloak çıkış sayfasının iframe'iyle OIDC front-channel; Okta ve Entra ID canlı denenmedi) |
| D-102 | 2026-09-14 | **v1.0 kapsamı: host agent'ı yalnızca Linux.** Windows ve macOS host agent'ı (metrik, envanter, keşif, loglar, servis kurulumu) v1.0 kapsamı dışında; "Veri ekle" akışı ve dokümanlar Linux, Docker ve Kubernetes kurulumlarını kapsar. APM agent'ları (Go, Node.js, Python, Java, .NET, PHP) OTLP ile her işletim sisteminden veri gönderebilir; .NET Framework/IIS Windows'ta CI ile test edilir. | D-003/D-005 ile uyumlu (ilk platform Linux); Windows/macOS agent'ı ayrı bir toplayıcı katmanı (WMI/ETW, launchd/IOKit), paketleme ve güncelleme hattı gerektirir ve v1.0'ı geciktirmemeli. | Kabul (v1.0 sonrası yol haritası maddesi) |
| D-103 | 2026-09-14 | **PHP soket erişimi ayrı `openlog-php` grubuyla, PHP-FPM pool kullanıcıları otomatik eklenir:** `php.sock` `0660` ve varsayılan grubu `openlog-php` (sistem grubu). Root `-reconcile` (her servis başlangıcında `ExecStartPre=+ … -apply`, deb/rpm postinstall, `install.sh`) grubu oluşturur, `openlog-agent`'ı ekler ve — `php_forwarder.grant_pool_users: false` (yalnız root'a ait config), `/etc/openlog-infra-agent/no-php-access` veya kurulumda `OPENLOG_AGENT_PHP_ACCESS=0` / `install.sh --no-php-access` yoksa — PHP-FPM pool dosyalarındaki (`/etc/php/*/fpm/pool.d`, `/etc/php-fpm.d`, Remi, cPanel `ea-php*`, Plesk, Alpine) `[pool]` bölümlerinin mevcut, root olmayan yerel `user` hesaplarını ve Apache/nginx kuruluysa `www-data`/`apache`/`nginx`'i ekler; üyeler asla çıkarılmaz. Yalnız kullanıcısı yeni eklenen ve aktif olan PHP-FPM (mod_php için Apache) servisleri `systemctl reload` edilir (restart yok). Agent grupları değiştiremez: her sync'te `php_access` (soket grubu, pool başına `ok`/`missing`/`opted_out`/`unsupported_user`) raporlar ve eksikleri bir kez loglar; backend son raporu `agent_hosts.php_access` (0064) içinde saklar, host Services sekmesindeki PHP-FPM kartı erişimi olmayan pool'ları ve düzeltmeyi (`systemctl restart openlog-infra-agent` veya `usermod -aG openlog-php <user> && systemctl reload php<v>-fpm`) gösterir. `openlog-php` yoksa eski `www-data`/`nginx`/`apache`/`php-fpm` sırası yalnız yedektir; uygulanamazsa bilgi seviyesinde tek log yazılır. | Hosting panellerinde (HestiaCP, cPanel, Plesk) her site kendi kullanıcısıyla çalışır; soketin `www-data` grubu bu pool'lara yazma izni vermediği için bir PHP 7.2 sitesinin span'ları `EACCES` ile kayboluyordu. `openlog-agent` grubu lisans anahtarını içeren `config.yaml`'ı okuyabildiği için PHP kullanıcılarına verilemez; ayrı grup yalnız sokete yazma izni verir (üyeler en fazla bu host adına span gönderebilir). Grup üyeliği süreç başlangıcında `initgroups` ile uygulanır: ön başlatma adımı ExecStart'tan önce çalıştığından agent grubu aynı başlangıçta alır; FPM reload'u master'ı yeniden çalıştırıp worker'ları yeniden oluşturduğu için yeterlidir. Root timer/path unit bilinçli olarak eklenmedi: yeni siteler bir sonraki servis yeniden başlatmasında veya elle eklenir, eksikler arayüzde ve logda görünür. |
