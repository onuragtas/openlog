# 06 — Yol Haritası

Süreler kalabalık ekip ve paralel çalışma varsayımıyla verilmiştir. Kilometre taşları kapsamla tanımlanır; tarih taahhüdü değildir.

## M0 — Sözleşmeler ve iskelet (2–3 hafta)

Amaç: Ekiplerin birbirini beklemeden çalışabileceği sözleşmeler ve uçtan uca çalışan en ince dilim.

- [x] Plan ve sözleşme belgeleri (`docs/plan`, `docs/contracts`)
- [x] ClickHouse şeması: metrics, logs, spans, hosts, inventory (Replicated + Distributed) ve `openlog-migrate`
- [x] `openlog-ingest`: OTLP HTTP + gRPC, license key → tenant, Kafka producer
- [x] `openlog-processor`: Kafka consumer group, OTLP → satır, envanter/keşif yönlendirmesi, batch insert
- [x] `openlog-api`: host listesi, host metrikleri, envanter, keşfedilen servisler, log arama, trace getirme
- [x] `openlog-allinone`, `openlog-loadgen`
- [x] `agents/infra`: sistem metrikleri, envanter (os, hardware, package, systemd_unit, listening_port, process, user, network_interface, mount, kernel_module), kural tabanlı keşif motoru + 29 başlangıç kuralı, OTLP/HTTP exporter, disk buffer
- [x] `deploy/compose` (`single` profil, uçtan uca doğrulandı)
- [x] `deploy/helm/openlog` (`cluster` profil) — kind (1+3 node) üzerinde Strimzi + Altinity operator'larıyla kuruldu, 7 hata düzeltildi
- [x] Agent'ın gerçek backend'e veri gönderdiği uçtan uca test (`make e2e`, kesinti ve 503 senaryoları dahil)
- [x] kind üzerinde ölçekleme ve chaos deneyi — ~118k kayıt/s kabul, tüm kesintilerde veri kaybı 0; processor ölçeklemesi ×1,4'te durdu (D-018). Ayrıntı: `docs/operations/benchmarks/2026-09-13-kind.md`

**Kabul kriteri:** Temiz bir Linux makinede agent kurulur; 2 dakika içinde API'den host'un metrikleri, envanteri ve keşfedilen servisleri sorgulanabilir. Aynı kurulum Helm ile 3 node cluster'da çalışır.

## M1 — Uçtan uca host izleme, v0.1 public release (6–8 hafta)

- UI: host listesi, host detayı (metrik grafikleri), envanter, keşfedilen servisler, log arama
- PostgreSQL: organizasyon, kullanıcı, roller, license/API key, oturum, davet, audit log (statik key'lerin yerine) ✅; davet e-postası + yeniden gönderme/süre ✅ (D-045); kayıt arayüzü, e-posta doğrulama, IP/e-posta başına sınır, CAPTCHA (Turnstile/hCaptcha), engellenen alan adı listesi ✅ (D-045); üyelikten çıkarma/rol değişikliğinin tüm pod'larda anında etkisi + çıkarılanın API key'lerinin iptali ✅ (D-046); audit log ekranı (filtre + sayfalama) ✅; key hash'lerinde HMAC (`OPENLOG_KEY_HASH_SECRET`, şeffaf yeniden hash, rotasyon) ✅ (D-044). `internal/config`'in okuduğu tüm env'lerin config.md + Compose (`.env.example`) + Helm'de bulunması ve bunu denetleyen test (`envcoverage_test.go`; sırlar chart Secret'ında) ✅; e-posta şablonları TR/EN, düz metin + HTML (`internal/mail/templates`, dil `Accept-Language`'tan, davette saklanır; migration 0050) ✅; quota ve PostgreSQL store entegrasyon testleri Docker ile ✅. CNPG Postgres kind'de denendi (2 instance, primary silindi: API çalışmaya devam etti, geçiş ~1 sn, tek 503; varsayılan `smartShutdownTimeout` 180 sn bağlantı havuzları yüzünden failover'ı ~3 dk geciktirdi → chart 15 sn) ✅. Kullanıcı dil tercihi (Ayarlar → Profil, `auto`/`en`/`tr`, arayüz tarayıcı dili yerine tercihle açılır) ve organizasyon varsayılan e-posta dili (admin/owner); e-posta dili: kullanıcı tercihi → organizasyon varsayılanı → saklanan istek dili → İngilizce (`users.locale_explicit`, `organizations.locale`; migration 0060; audit; D-095) ✅
- Agent: process ve container metrikleri ✅, log toplama (dosya + journald) ✅, rpm envanteri ✅, container envanteri (Docker) ✅, D-Bus systemd ✅ (unit durumu, aktif olduğu zaman, `NRestarts`, bellek/CPU muhasebesi, template instance'ları; bus yoksa dosya tabanlı envantere düşer), keşif kataloğunun tamamı
- Ölçekleme ve chaos testleri CI'da; 04-cluster.md'deki açık konular karara bağlanır
- Paketleme: deb/rpm/install.sh, imzalı release'ler
- Güvenlik: Kafka (TLS/mTLS + SASL PLAIN/SCRAM), ClickHouse (TLS), PostgreSQL (TLS) ✅ (`make tlstest`); API/alert sorguları için salt okunur ClickHouse kullanıcısı (readonly=2, yalnızca `openlog.*` SELECT; compose users.d + Helm CHI) ✅ (D-047); tenant başına sorgu limitleri (`max_memory_usage`, `max_rows_to_read`, `max_bytes_to_read`, `log_comment`/`quota_key` = tenant; aşımda `422`/`429 resource_exhausted`) ✅ (D-047); Kafka/ClickHouse/PostgreSQL istemci sertifikalarının dosya değişince yeniden başlatmadan yüklenmesi ✅ (D-048); Altinity operator modunda ClickHouse sunucu TLS'i (`clickhouse.tls.server`) ✅ (D-049). tenant sorgu limitlerinin organizasyon ayarı olarak yönetilmesi (`org_query_limits`, `GET/PUT/DELETE /api/v1/usage/query-limits`, Ayarlar → Kullanım; öncelik: env varsayılanı < plan < organizasyon < `OPENLOG_QUERY_TENANT_LIMITS`; api ve alert pod'larında 30 sn önbellek) ✅ (D-084). Kalan: Helm TLS'i kind'de denendi (cert-manager CA, strict doğrulama, openlog 9440'ta istemci sertifikasıyla, sertifikasız istemci reddedildi) ✅; operator modunda Keeper (9281 + Raft TLS) ve replikasyon (interserver HTTPS 9010) TLS'i chart'ta ve kind'de doğrulandı ✅ (D-093). Kalan: `disableInsecure` (operator 0.27.3 strict modda istemci sertifikası sunamıyor; HTTPS erişimiyle denenmedi), plaintext Keeper portu 2181'in kapatılması (operator probe'u kullanıyor)
- UI iskeleti (`web/`): React + TypeScript + Vite (D-017) — M0'da kuruldu; M1'de route bazlı code-splitting ✅ (ilk yükleme JS ≈211 kB gzip; uPlot yalnızca grafikli ekranlarda, React Flow + dagre yalnızca servis haritasında ayrı chunk), log listesi sanallaştırma + "daha eski loglar" sayfalama ✅, waterfall sanallaştırma + alt ağaç açma/kapama + span arama/vurgulama ✅, grafiklerin `setData`/`setSize` ile yerinde güncellenmesi ✅ (vitest: render sayısı, uPlot örneğinin yeniden kullanımı, unmount temizliği); kalan: gerçek backend ile doğrulama, logs API'sinde opak cursor (şu an `to` = son satırın zaman damgası; aynı nanosaniyede `limit`'i aşan satırlara ulaşılamaz)
- API: bilinmeyen host için tüm host uç noktalarında `404` ✅; host log sekmesinde kaynak/dosya/servis/unit filtreleri ✅
- `make e2e` genişletildi: agent logları (rotasyon dahil), process metrikleri, container envanteri/metrikleri, gerçek backend'e karşı Playwright arayüz testi (`E2E_UI=1`) ✅; rotasyon anında okunmamış satırların eski dosyadan okunmasının zorlanması (10 000 satırlık burst + `mv`, her satır tam bir kez; rate limit altında eski dosyanın erken kapanması hatası düzeltildi) ✅, journald uçtan uca (target'ta bağımsız `systemd-journald`, `systemd-cat`/`logger` → journalctl; ayrıca gerçek journal dosyasıyla Linux entegrasyon testi) ✅
- Processor: doğrudan shard insert (D-018) ✅; tekrar gönderimde aynı sınırları üreten chunk'lar ✅; consumer lag metrikleri + alarm kuralları ✅; retry'da `metrics_1m`/`trace_index` çift sayım hatası düzeltildi ✅
- Ingest/processor `/readyz` topic kontrolü (D-019) ✅
- Çok node'lu ortamda 3+ ingest ve 4+ processor ile ölçüm tekrarı (direct vs distributed)
- Agent: Debian'da `redis-server → redis-check-rdb` gibi symlink'lerde kullanıcıya gösterilecek servis adı/instance ✅ (`command` + yeni `display_instance` = çağrılan yol; `instance` anahtarı değişmedi, mevcut varlıklar ve metrik serileri korunur)

- Sürümler ve otomatik güncelleme (09, D-025–D-029) ✅: `libs/release` (imza/manifest), `openlog-release` + imzalı yerel release, deb/rpm + `install.sh`, GitHub Actions (CI + release; GitHub'da henüz çalışmadı), agent kendini güncelleme + otomatik geri dönüş, filo politikası/dalgalar/durdurma + Filo ekranı, backend sürüm kontrolü + UI bandı, expand/contract migration + N/N+1 testi, Compose updater (yedek → migrate → yeniden oluştur → geri dönüş), Helm updater CronJob (gerçek cluster'da denenmedi)
  - Manuel güncelleme (D-041): Ayarlar → Organizasyon → "Sürüm ve güncellemeler" sayfasında "Şimdi kontrol et" (api kontrolü + updater'a istek, 30 sn hız sınırı) ve "Şimdi güncelle" (`update_requests` tablosu, Compose updater 10 sn'de alır, `notify` modunda da uygular, bakım penceresi dışı için açık onay; sayfa api yeniden başlarken bağlantıyı kaybetmeden izler). Filo sayfasında "Şimdi dağıt" (dalga beklemelerini atlar; dalga bekleyen agent'lar 60 sn'de bir sync yapar). Kubernetes CronJob istekleri bir sonraki çalışmasında işler.

### Ara adım — Tüm sistemin Docker ile ayağa kalkması (M1 kapanışı) ✅ 2026-09-13

Sonuç: `make stack-demo` sıfırdan 9,5 dk'da geçti (`docs/operations/local-stack-demo.md`). Agent kurulumundan ilk veriye 4–5 sn; agent'lar 0.9.0 → 0.9.1 kademeli güncellendi (129 sn, 2/2 başarılı); backend compose updater ile 0.9.1'e 7 sn'de güncellendi, veri korundu; tam yeniden başlatmada veri kaybı yok. Bozuk imajda geri dönüş ✅ (2026-09-14, `make updater-acceptance`: imzalı ama bozuk 0.9.2 gerçek bir derleme ve ek bir expand migration (9003) içeriyor; updater migration'ı uyguladı, health başarısız oldu, 0.9.1'e geri döndü; veri, sorgular ve yeni ingest korundu, `failed_versions`/audit log ve UI mesajı "0.9.2: failed before on this installation"; paylaşılan `openlog` projesini kullanan `stack-demo` içinde değil, aynı compose dosyası ve updater ile `openlog-updtest` projesinde). Gerçek migration içeren sürüm geçişi ✅ (`UPDTEST_FROM_IMAGE=ghcr.io/onuragtas/openlog:0.1.9`: yayınlanmış 0.1.9'un kendi updater'ı çalışma ağacı derlemesine geçti, PostgreSQL 0010…0056 (16) ve ClickHouse 0020…0050 (7) migration'ı backup → expand → recreate → health → contract sırasıyla uygulandı, veri korundu). Compose değişikliği sonrası testler tekrar koşuldu ✅: `make e2e` geçti (536 sn), `make updater-acceptance` geçti (ilk koşuda testteki bir sayım hatası düzeltildi), `make mixed-version` ilk koşuda gerçek bir hata buldu — `openlog-allinone` başlangıç migration'ları sürerken ve API portu dinlemeden `/readyz` 200 dönüyordu (compose `--wait`, updater health adımı ve Kubernetes probe'ları erken "hazır" görüyordu); düzeltme: `migrations` kapısı ve `api_listener`/`ingest_listener` kontrolleri (`internal/admin`), ardından geçti. Kalan (bu ara adım): `stack-demo` senaryosunun kendisinde bozuk sürüm fazı. Helm chart'ın release'e girmesi ✅ (`openlog`/`openlog-agent` `.tgz` release asset'i, imzalı manifest'te `helm_charts`, `HELM_OCI_PUSH` ile isteğe bağlı GHCR OCI push), updater mesajlarının Türkçeleştirilmesi ✅ (durum ve istek mesajlarında `message_code`/`message_params`, UI `update.messages/steps/states` çevirileri, kodsuz eski belgelerde İngilizce metin), compose değişikliği sonrası `e2e`/`autoupdate`/`mixedversion` testlerinin tekrar koşulması.

Otomatik güncelleme, filo, TLS ve M1 kalanları birleştirildikten sonra, sıfır durumdan tek komutla:
- `deploy/compose` ile PostgreSQL, Kafka, ClickHouse, openlog (ingest + processor + api + UI), bootstrap, `openlog-updater` ve gerçek bir `openlog-infra-agent` host'u ayağa kalkar; tüm healthcheck'ler yeşil.
- Tarayıcıda: e-posta ile giriş → host listesi → grafikler, servisler, envanter, loglar → Ayarlar → Filo ekranı (agent sürümü, politika) → sürüm bilgisi.
- Agent sıfırdan kurulum → ilk veri < 2 dk; imzalı yerel release ile agent otomatik güncellemesi ekranda görülür.
- Ekran görüntüleri ve adım adım çıktı raporlanır; bulunan sorunlar düzeltilir.

## M2 — Alarm, entegrasyonlar, APM başlangıcı (paralel, ~6 hafta)

- `openlog-alert`: eşik kuralları, incident, Slack/e-posta/webhook
  - ✅ (D-030, `docs/contracts/alerting.md`): metrik/log/veri yok/keşif/APM kural tipleri, lease ile kural paylaşımı (lease sahibi öldürülünce 22,8 sn'de devir, tek incident), outbox + idempotent teslim, Slack/e-posta (STARTTLS)/HMAC imzalı webhook/Teams, susturma, önizleme grafiği, "bu metrikten alarm oluştur" kısayolu; e2e'de tek açılış + tek kapanış bildirimi doğrulandı.
  - ✅ `apm_no_data` kuralı ("APM servisi raporlamayı bıraktı", §2.7); değerlendirme özetleri ClickHouse `alert_evaluations`'a (doğrudan shard insert, kural başına tek shard, 30 gün TTL) + `GET /alerts/rules/{id}/evaluations` + kural ekranında geçmiş grafiği (§3.6); tekrarlayan susturma (gün + saat + saat dilimi veya `FREQ=WEEKLY;BYDAY=…`, DST testli, eski sürümlerle uyumlu, §5.2).
  - ✅ Aylık/tatil istisnalı takvimler (D-056): `FREQ=MONTHLY` + `BYMONTHDAY`/`BYDAY` (sıralı `1MO`, `-1FR`)/`BYSETPOS`, `exdates` ve organizasyon bazında adlandırılmış tatil takvimleri (`/alerts/holiday-calendars`, Postgres 0015, `YYYY-MM-DD` veya her yıl `MM-DD`), `upcoming` ve `POST /alerts/mutes/preview`; DST değişim günlerinde test edildi; eski dispatcher'lar aylık kuralları susturmaz (§5.2). UI: aylık kural formu, istisna tarihleri, takvim seçimi, sonraki tekrarlar, takvim yöneticisi.
- Agent entegrasyonları: nginx, Redis, MySQL, PostgreSQL, Docker (keşif ile otomatik açılma)
  - ✅ (D-031): keşifle yaşam döngüsü, uç nokta türetme, `env:`/`file:` kimlik bilgileri, gerçek durum (`enabled`/`needs_configuration`/`error` + ipucu), OTel Collector receiver isimleriyle nginx (4), Redis (28), MySQL/MariaDB (28), PostgreSQL (21) metrik; gerçek container'larda doğru/eksik/yanlış parola senaryoları doğrulandı. Docker: OTel'de motor seviyesi metrik adı olmadığı için yalnızca erişilebilirlik.
  - ✅ SSH'sız yapılandırma: docker.sock erişimi olmayan konteyner servislerinde uç nokta türetme (`/proc/<pid>/net` + `docker-proxy`, son çare loopback varsayılan port), nginx stub_status yolu hatırlanır ve https doğrulamasız deneme yalnızca loopback'te; UI'dan host/organizasyon bazında entegrasyon ayarı (D-039, parola şifreli, agent sync ile revizyonlu teslim, agent yeniden başlamadan uygular, `integrations.remote_config: false` ile kapatılır); paketler `openlog-agent`'ı varsayılan olarak `docker` grubuna ekler (D-040, `--no-docker-access` / `OPENLOG_AGENT_DOCKER_ACCESS=0`). Kalan: gerçek sunucuda (deb + Docker'da nginx/redis) uçtan uca doğrulama.
  - Güvenlik düzeltmesi: komut satırı maskeleme `--requirepass`/`--masterauth` gibi sonu parola kelimesiyle biten bayrakları kaçırıyordu → düzeltildi, test + sözleşme §3.5 güncellendi.
  - ✅ MySQL io-wait metrikleri mysql:8.4 ve mariadb:11 container'larında doğrulandı (top-N sıralaması sabitlendi, `performance_schema=OFF` kısmi toplama + ipucu; mysqlreceiver'ın index sorgusundaki etiket kayması §6.4'te not edildi); nginx Plus API / VTS otomatik algılama (`nginx.status.source`, `nginx.http.response.status`, upstream peer metrikleri; yalnızca fixture, canlı nginx denenmedi); Redis Cluster (`CLUSTER INFO/NODES`, `redis.cluster.*`, 3 master'lı gerçek cluster'da doğrulandı); `pg_stat_statements` (opt-in `query_stats`, top N, literal maskeleme, postgres:16'da doğrulandı); Docker entegrasyonu CRI-only hostlarda yanlış "enabled" demiyor (`DockerErr`).
  - ✅ Entegrasyon panelleri ve önerilen alarmlar (UI): Entegrasyonlar sayfasında entegrasyon türüne göre filo panosu (örnek başına KPI kartları, yapılandırma gerekenlere "Yapılandır" bağlantısı), panelde örnek seçici, Docker motor erişilebilirlik kartı, isteğe bağlı grafikler (nginx durum kodları/upstream, Redis Cluster slotları, PostgreSQL kilitleri) ve en yoğun sorgular tablosu; sunucu tarafı şablon kataloğu (D-055, `GET /alerts/templates`, `POST /alerts/templates/{id}/render`, tr/en, host/konteyner/APM/entegrasyon, oran eşikleri örneğin güncel değerinden) ile önizlemeli tek tık kural oluşturma, "düzenleyicide özelleştir", Alarmlar › Şablonlar sekmesi.
- APM backend: span → transaction türetme, RED metrikleri, Apdex, servis listesi, trace waterfall (OTel SDK ile)
  - ✅ (D-032, `docs/contracts/apm.md`): transaction/hata/örnekleme ağırlığı tanımları, `apm_*` ClickHouse tabloları + MV'ler (2 shard'da doğrulandı), shard başına edge eşleştirme işi, servis listesi/genel bakış/transaction/hata gelen kutusu/DB sorguları/servis haritası/trace arama + trace logları, host ↔ servis bağlantısı, Apdex T ayarı. `test/apmdemo` (Node + Go + PHP) ile API sayıları ham span'larla birebir (percentiller <%9,1). Processor `hosts` ezme hatası düzeltildi.
  - ✅ `make stack-demo`'ya APM ve alarm fazları eklendi; geç span'lar için ayarlanabilir lookback (≤24 sa) + önceki günü gece toplu yeniden eşleştiren catch-up (`openlog_apm_link_late_calls_total`); `OPENLOG_APM_RETENTION_DAYS` (migrate `MODIFY TTL ON CLUSTER`, 2 shard/3 replikada doğrulandı); bağlantı yönetimi span'ları (`sql.connector.connect`, `sql.conn.reset_session` …) DB sorgularından çıkarıldı (apm.md §7).
  - ✅ Catch-up sonrasından da geç gelen span'lar (D-054): processor geç span'lerin client dakikalarını ClickHouse `apm_relink_queue` tablosuna (0020) yazar, api lideri 5 dakikada bir en fazla 120 dakikayı tüm shard'larda yeniden bağlar; metrikler `openlog_apm_relink_*`; 2 shard'lı canlı testte doğrulandı.
- Go ve PHP agent geliştirmelerinin başlaması
  - Go agent (`agents/go`, D-033) ✅: `openlog.Start`, HTTP/SQL/slog/gRPC/chi/gin/echo, runtime metrikleri, infra agent ile aynı `host.id`, canlı ortamda span/log/metrik doğrulandı. Örnekleme oranı `tracestate` (`ot=th`) ile servisler arası taşınıyor ✅ (paylaşılan openlog'da %25 oranla 400 istek → 432 ağırlıklı, +0,9 sd); Go modül tag'leri release'e bağlandı ✅ (`make release-prepare`, `release.yml` kontrol + tag job'ları; GitHub'da henüz koşmadı). W3C random bayrağı (kök span'lar) ve isteğe bağlı `rv` yazma (`OPENLOG_SAMPLING_RV`) ✅; container içindeki uygulamalarda host eşleşmesi ✅ (infra agent `host.id`'yi `/run/openlog-infra-agent/host-id` dosyasına yazar, Go agent önce onu okur; container'a salt okunur mount edilir)
  - Backend hatası (Go agent testi buldu): processor uygulama kaynaklarından da `hosts` satırı yazıyordu → ✅ düzeltildi, yalnızca `openlog.entity.type=host` kaynakları yazıyor
  - PHP agent (D-035–D-038): karşılaştırmalı ölçüm yapıldı (`agents/php/docs/decision.md`); karar: kendi C extension'ımız, PHP 7.1+, fonksiyon seviyesi ayrıntı, forwarder infra agent içinde. Infra agent `php_forwarder` modülü ✅ (unix datagram, doğrulama, parça birleştirme, host bağlantısı, kesintide disk buffer; paylaşılan openlog'da `php-shop` servisi ve trace'leri görüldü). Extension faz 1 ✅ (`agents/php/ext`): 8.2+ Observer API, 8.0/8.1 Observer + `zend_execute_internal`, 7.x `zend_execute_ex`; Laravel/Symfony/Slim/WordPress/CodeIgniter/Yii 2/düz PHP, PDO/mysqli/pgsql/phpredis/Predis, curl/curl_multi/streams (+ `traceparent`), hatalar, örnekleme tabanlı fonksiyon izleyici; phpt matrisi 7.1–8.4 NTS + 8.3 ZTS + musl geçti; 5 demo uygulama paylaşılan openlog'da. Ek yük (D-057) kısmen ✅: sıcak yol yeniden yazıldı (`ol_text.c`, sorgu önbelleği, örnekleyici ısınması); callgrind istek başı ek komut düz PHP'de 1,12 M → 0,58 M, Laravel'de değişmedi (1,78 M) çünkü maliyet motorun Observer API'si (8.0–8.3'te çağrı başı ~100 komut); HTTP (FPM+nginx+wrk, Laravel, PHP CPU/istek medyan): önce +455 µs (kapalı) / +439 µs (açık), sonra +400 µs / +197 µs, yalın mod (`openlog.userland_hooks=0`, framework kancaları yok) +123 µs (%4,2; komut +%3,7). **Tam modda hedef (≤%3 / ≤%7) hâlâ tutmuyor**; RPS gürültüsü ±%10–20, ayrılmış makinede doğrulama gerekli. ASan/UBSan phpt (0 rapor, CI'da 8.3-asan) ✅, `ol_text.c` fuzz (CI 120 sn) ✅, 10 dk soak (332k istek, RSS artışı yok) ✅. Birlikte çalışma ✅: Xdebug coverage, New Relic, OPcache JIT tam geçti; ddtrace 26/28 (ddtrace curl `traceparent`'ını değiştiriyor, Predis çift span). Guzzle 7 async/Pool ✅ (gerçek kütüphaneyle test). Uzun ömürlü worker'lar (D-058) ✅: Octane, RoadRunner, Swoole/OpenSwoole (eşzamanlı coroutine'lerde karışma yok, test edildi). Paketleme (D-059) ✅: ABI başına 36 modüllü tarball + deb/rpm/apk, `openlog-php-install`, Debian 12 / Alma 9 / Alpine 3.20 kurulum testleri geçti. Filo ile dağıtım (D-082, D-083) ✅: release tool `php-agent` bileşeni (tar.gz/deb/rpm/apk, imzalı manifest), `release.yml` yerel amd64/arm64 `php-agent-artifacts` işleri (CI dry run'da küçük alt küme); infra agent PHP çalışma ortamı envanteri, `php_agent` yapılandırması + sync ile uzaktan ayar, yetkisiz indirme/doğrulama, `-apply` içinde root kurulum, sağlık kontrolü ve geri alma, `mode: off` ile kaldırma, `openlog.agent.php_agent.operations` + `openlog.php_agent.version`; backend politika bölümü (dalgalar, bakım pencereleri), host bazında mod, Fleet sayfasında PHP sütunu. Debian 12 systemd e2e: auto → php8.2-fpm'e kurulum + reload + span akışı, bozuk sürümde geri alma, off ile kaldırma. FrankenPHP ✅: worker modu (`frankenphp_handle_request()` callback'i başına bir transaction, istek başına `$_SERVER` öznitelikleri, durum/istisna, `exit()` ve worker yeniden başlatma; worker betiğinin kendisi gönderilmez) ve klasik mod; gerçek FrankenPHP sunucusuyla `tests/044-frankenphp.phpt`, matris etiketi ve CI `php-ext (8.4-frankenphp)`. 044'teki aralıklı hata (ilk istek yanıtsız, SIGTERM ile durmama) extension değil FrankenPHP ≤1.12.7 başlangıç yarışı: Caddy `frankenphp.Init()` istek kanalını oluşturmadan dinlemeye başlıyor, o aralıktaki istek nil kanalda sonsuza dek bekliyor (php/frankenphp#2612); extension'ın ~10 ms başlangıç maliyeti pencereyi büyütüyor. Test artık `FrankenPHP started` log satırını bekliyor (önce 7/30 hata, sonra 35/35 geçti). Soket erişimi (D-103) ✅: `php.sock` grubu `openlog-php`; root `-reconcile` grubu oluşturur, `openlog-agent`'ı ve PHP-FPM pool kullanıcılarını (HestiaCP/cPanel/Plesk site kullanıcıları, Apache/nginx kullanıcısı) ekler, yalnız etkilenen FPM servislerini reload eder; agent sync'te `php_access` raporlar, Services sekmesi erişimi olmayan pool'ları ve düzeltmeyi gösterir (panelli sunucuda PHP 7.2 sitesinin span'larının `EACCES` ile kaybolması üzerine). Kalan: ayrılmış makinede ek yük ölçümü (M3).
- Konteynerler (D-043) ✅: infra agent konteyner durumu/sağlığı/yeniden başlatma sayısı ve Docker Compose / Kubernetes adları (`openlog.container.status`, `container.restarts`, `docker.compose.*`, `k8s.*`), durmuş konteynerler 24 sa görünür; ClickHouse `containers` + `apm_service_containers` MV'leri (schema 0009), `logs` için `container.id`/compose servisi indeksleri; `/api/v1/containers*` + servis ↔ konteyner uç noktaları; UI'da Konteynerler sayfası (compose servisine göre gruplama, filtreler, mobilde kartlar), detay (CPU/bellek/ağ/disk grafikleri, APM servisleri, loglar, nitelikler), host sayfasında Konteynerler sekmesi, APM servis sayfasında konteynerler. Konteyner logları otomatik (`logs.containers`): json-file dosyası (rotasyon, parça birleştirme, ofset) veya Docker API akışı (çoklanmış akış, `since` ile devam), JSON gövdeden trace_id/span_id. `test/localagents`: demo uygulamalar `container.id` gönderir, `docker-host` agent Docker motorunu izler. containerd/CRI-O meta verisi (CRI) ✅: `containers.cri_sockets` üzerinden CRI `runtime.v1` (ListContainers/ContainerStatus; grpc bağımlılığı yok, HTTP/2 + protowire), `container.runtime` `containerd`/`cri-o`, k8s pod/namespace/konteyner adları, yeniden başlatma sayısı, Docker ile birleştirme; CRI log dosyaları (`P`/`F` parça birleştirme). Konteyner loglarında çok satırlı gruplama ✅: `openlog.logs.multiline` etiketi veya `include[].multiline_start` (etiket öncelikli), stdout/stderr ayrı, 2 sn boşta flush, `max_line_bytes`. Kubernetes pod görünümü ✅ (Kubernetes bölümündeki pod listesi/detayı `/kubernetes/pods*`, D-070/D-071; APM servis sayfası servisin pod'larına bağlantı verir), servis haritasında host/konteyner sayısı ✅ (servis düğümünde rozet + ipucu, "Host ve konteynerler" aç/kapa düğmesi tarayıcıda hatırlanır)
- Entegrasyon panelleri ve önerilen alarm şablonları (UI) ✅ (yukarıda, entegrasyonlar maddesi)

## M3 — APM GA ve sorgu dili

- Go, PHP, Node.js agent'ları; hata gelen kutusu, service map, log ↔ trace geçişi
  - APM GA (D-068, D-069; `docs/contracts/apm.md` §3.3–3.4, §5.1, §11–12): hata gelen kutusu iş akışı (durum, atama, yorum, toplu işlem, filtreler, sürüm farkındalıklı otomatik yeniden açma, etkilenen sürüm/host/container/transaction, `apm_error` alarm kuralı: yeni grup / geri dönüş), parmak izi v2; log ↔ trace iki yönlü geçiş (log satırından trace/span'e, trace/span/transaction'dan loglara) ve log API imleçli sayfalama; service map GA (ortam/namespace filtresi, host/container sayıları, transaction yolu vurgulama, kullanıcı başına yerleşim, büyük graf); dağıtım işaretleri ve öncesi/sonrası karşılaştırma. Backend + ClickHouse/PostgreSQL entegrasyon testleri ✅; web arayüzü ✅ (hata gelen kutusu + organizasyon geneli sayfa, log ↔ trace bağlantıları, imleçli log sayfalama, harita filtre/yol/yerleşim, dağıtım işaretleri ve karşılaştırma, `apm_error` kural formu; MSW mock'ları, vitest ve Playwright `e2e/apm-ga.spec.ts`).
  - Node.js agent (`agents/node`, `openlog-node`, D-060–D-062) ✅: OTel JS SDK dağıtımı, `--require`/`--import openlog-node/register` ve `start()` API, Go agent'la aynı ortam değişkenleri, host.id zinciri, container.id, örnekleme (`ot=th`/`rv`, random bayrağı; Go agent'tan üretilen fikstürlerle doğrulandı), http/fetch/express/fastify/koa/NestJS/pg/mysql2/redis/ioredis/mongodb/graphql/grpc/aws-sdk, route adlandırma, Go ile aynı SQL temizleme, pino/winston/console log korelasyonu, semconv runtime metrikleri; testler Node 18 (birim), 20/22/24 (birim + uçtan uca OTLP yakalama), gerçek PostgreSQL/MySQL/Redis ile instrumentation testi; CI `node-agent` job'u ve `release.yml` npm yayını (`NPM_TOKEN` gerekli); `test/localagents` frontend demosu paketi kullanıyor. NestJS 12 ✅ (ESM-only, upstream Nest instrumentation <12): `--import` ile express/fastify instrumentation'ı route adını veriyor (doğrulandı), isteğe bağlı `OpenLogNestInterceptor` (`openlog-node/nest`) route'u ve `nestjs.controller`/`nestjs.callback`'i kendisi yazıyor; NestJS 12 ESM e2e testi (express + fastify). Kalan: paylaşılan openlog'da canlı doğrulama, ilk npm yayını, NestJS 12'de Nest seviyesinde span'ler (upstream), ayrılmış makinede ek yük ölçümü.
- NRQL benzeri sorgu dili, özel dashboard'lar
  - OQL sorgu dili ve özel dashboard'lar (D-063–D-065; `docs/contracts/oql.md`, api.md "Query language (OQL)"/"Dashboards") ✅: parser/plan/parametreli SQL (`internal/oql`, golden + fuzz + güvenlik + ClickHouse entegrasyon testleri), `POST /query`, `/query/validate`, `GET /query/schema`, dashboard CRUD/import/export/kopyalama/widget ekleme (PostgreSQL 0020), `oql` alarm tipi (0021), web: sorgu konsolu (CodeMirror, otomatik tamamlama, geçmiş), dashboard ızgarası, değişkenler, widget düzenleyici. Widget'lar arası çapraz filtre ✅ (facet değerine tıklama → URL'de `filters`, plan seviyesinde parametreli `WHERE`, olay tipinde nitelik yoksa atlanır; oql.md §7), dashboard sürüm geçmişi ✅ (Postgres 0053, son 50 sürüm, fark özeti, geri yükleme = yeni sürüm; D-086), paylaşım bağlantıları ✅ (organizasyon ayarıyla isteğe bağlı, süreli, iptal edilebilir, hash'li token, audit, hız sınırlı salt okunur `/shared/dashboards/{token}` görünümü; Postgres 0054; D-087) ve zamanlanmış raporlar ✅ (günlük/haftalık, saat dilimi, tr/en, üye/izinli alan adı alıcılar, lider işi, dönem başına tek gönderim, HTML tablolar; Postgres 0055; D-087) — Go birim + PostgreSQL entegrasyon testleri, API testleri, vitest, Playwright. Raporlarda PNG grafik ✅ (ayrı `openlog-renderer` imajı: headless Chromium/chromedp, iç API + `OPENLOG_RENDERER_TOKEN`, isteğe bağlı mTLS; api liderinin imzaladığı 5 dk'lık rapora bağlı render token'ı ile `/print/dashboard` görünümü; widget başına CID satır içi PNG, hata/limitte HTML tabloya dönüş; Compose `renderer` profili, Helm Deployment + ClusterIP + NetworkPolicy, release'te çoklu mimari imaj; birim + konteyner entegrasyon testleri; D-097), küme geneli paylaşım hız sınırı ✅ (PostgreSQL `rate_limit_counters` kayan pencere, pod içi toplu yazma + sınıra yakın eşzamanlı yazma, lider budama; iki api sunucusu tek veritabanıyla testli; Postgres 0061; D-096), rapor alıcılarının üyelikten çıkınca (API ve SCIM) aynı transaction'da listeden temizlenmesi + boş kalan raporun kapatılması ve audit ✅ (Postgres 0062; D-096).
- Katmanlı saklama (S3)
  - ✅ (D-066, D-067; `docs/operations/tiered-storage.md`): ClickHouse storage policy `openlog_tiered` (yerel `default` → isteğe bağlı `warm` → S3 `cold` + sınırlı cache, zero-copy kapalı, replika başına önek), tablo sınıfı başına retention'dan bağımsız taşıma günleri (`OPENLOG_STORAGE_*`), openlog-migrate'in idempotent `ON CLUSTER` politika/TTL adımı (APM retention ile birleşik, `-plan`), `openlog-admin storage status` (volume/tablo bazında bayt, bekleyen/başarısız taşıma, detached part), Compose `storage-tiered.xml` + MinIO profili `tiered` + kimlik bilgisi dosyası/IAM, Helm operator modunda CHI politikası + Secret/IRSA + warm PVC, `make tieredtest` (MinIO + 2 shard × 2 replika: warm/cold taşıma, Distributed okuma, yeniden başlatma, soğuk part silme, S3 kesintisi ve dönüş, kapatma) ✅. Kind'de (in-cluster MinIO) denemede sıra hatası bulundu ve düzeltildi: tiering'i açan upgrade'de pre-upgrade migrate hook'u politika CHI'ye gelmeden çalışıp sonsuza kadar bekliyordu (migrate artık canlı CHI'de politika yoksa taşımayı atlıyor, sonraki upgrade uygular). Kalan: Helm operator modunun uçtan uca (MATERIALIZE TTL, Distributed okuma, `storage status`) gerçek cluster'da — Docker diski dolduğu için tamamlanmadı — ve AWS S3/SSE-KMS/IRSA ile denenmesi, tiering açıkken `BACKUP … TO S3` geri yükleme provası ✅ (2026-09-14, `TIEREDTEST_BACKUP=1 make tieredtest`: 2 shard × 2 replika, parçalar `warm`/`cold`'dayken `BACKUP DATABASE openlog ON CLUSTER … TO S3` 2,8 sn, `DROP DATABASE … SYNC` + `RESTORE` 3,3 sn; düz tablolarda satır sayıları ve Summing/Replacing tablolarında mantıksal kontroller aynı, soğuk sorgu okunuyor, geri yüklenen parçalar önce sıcak diske düşüp TTL ile tekrar S3'e taşındı, TTL adımı no-op, ardından S3 kesintisi/kapatma fazları geri yüklenmiş veritabanında geçti; S3 kesintisinden hemen sonra BACKUP bir süre `S3_ERROR` ile başarısız oluyor — belgelendi, `tiered-storage.md` "Backups and restore"). Soğuk veriye dokunan sorgular için API'de ayrı hata kodu (`503 storage_unavailable`, `retryable: true`, UI mesajı) ✅.

## M4 — SaaS lansmanı

- Multi-tenant production, organizasyon/rol/SSO, kota ve faturalama
  - SSO ve SCIM ✅ (D-077, D-078; `docs/operations/sso.md`, api.md "Single sign-on"/"SCIM"): organizasyon başına OIDC (PKCE, state/nonce, JWKS önbelleği + rotasyon, algoritma allowlist'i, email_verified) veya SAML 2.0 SP (metadata, imzalı assertion, XML signature wrapping ön kontrolü, SHA-1 reddi, Postgres replay önbelleği, isteğe bağlı IdP-initiated + RelayState allowlist'i), DNS TXT / e-posta ile alan adı doğrulama (yalnız doğrulanmış alan adındaki kimlikler kabul edilir), JIT, grup → rol eşlemesi, organizasyona bağlı SSO oturumu + en uzun oturum süresi, test girişi, kilitlenme korumalı zorunlu SSO + break-glass sahipleri, SCIM 2.0 Users/Groups (anında üyelik kaldırma, oturum ve API anahtarı iptali), audit olayları (PostgreSQL 0030), web: Ayarlar → Tek oturum açma sihirbazı, giriş sayfasında "SSO ile giriş yap"; testler: birim (ID token / assertion doğrulama, wrapping, replay), SCIM uyumluluk alt kümesi, handler, PostgreSQL entegrasyon, Keycloak 26 uçtan uca (OIDC + SAML + IdP-initiated + zorunluluk + SCIM), Vitest + Playwright (MSW). Ek olarak ✅ (D-088, D-089): SAML Single Logout (SP ve IdP kaynaklı, imzalı LogoutRequest/LogoutResponse, HTTP-Redirect ve HTTP-POST, EncryptedID) ve OIDC RP-initiated logout (id_token_hint, post_logout_redirect_uri izin listesi), arayüzde "Her yerden çıkış (IdP)"; organizasyon başına birden fazla bağlantı (alan adı → bağlantı yönlendirmesi, bağlantıya özel rol eşlemesi ve zorunluluk; PostgreSQL 0056); SCIM ile e-posta değişikliği (doğrulanmış alan adı, `409 uniqueness`, denetim); sahiplenilmiş alan adında self-servis kayıt/davetin SSO'ya yönlendirilmesi; OIDC JWKS/discovery ve SAML metadata ön-yenilemesi için lider seçimli arka plan işi (bağlantı sağlığı, metrikler); şifreli assertion (AES-GCM/RSA-OAEP) Keycloak ile uçtan uca. Ek olarak ✅ (D-098): OIDC Back-Channel Logout 1.0 ve Front-Channel Logout 1.0 (bağlantı başına URI, `logout_token` doğrulama + `jti` replay önbelleği, `sid`/`sub` ile oturum sonlandırma, iframe için `frame-ancestors` issuer kökeni), SAML SOAP back-channel logout (imzalı LogoutRequest/LogoutResponse), URL'den yenilenen SAML metadata'sının sabitlenmiş sertifikayla XML imzası doğrulaması (wrapping/kurcalama reddi, imzasız metadata için açık izin, değişen imzalayan veya imzasız metadata'da değişen sertifika için yönetici onayı; PostgreSQL 0063); Keycloak 26 ile uçtan uca.
  - Kota ve kullanım ölçümü — uygulandı (D-079, D-080, D-081): saklanan satırlardan idempotent ölçüm (`usage_*` MV'leri, processor ingest baytı, `system.query_log` sorgu işlemi), yapılandırma olarak planlar + organizasyon override'ları (`org_plans`), dakikalık değerlendirme, %80/%100 banner ve sahip e-postaları, SaaS modunda ingest `429` + tenant başına hız sınırı, tenant başına saklama (en uzun plan TTL'i + partition kapsamlı silme), Settings → Kullanım ve plan (grafik, en çok servis/host, ay sonu tahmini, CSV/JSON dışa aktarma, süper yönetici plan ataması); `docs/contracts/usage.md`, `docs/operations/saas.md`. Faturalama: sağlayıcıdan bağımsız `internal/billing` arayüzü, `noop`, günlük idempotent kullanım gönderimi ve webhook → plan eşlemesi; gerçek sağlayıcı (Stripe/iyzico/Paddle) seçilmedi
  - SaaS operasyonları — uygulandı (D-105, D-106; `docs/operations/saas.md` §8–§11, api.md "SaaS operations"): süper yönetici konsolu `/operator` (organizasyon listesi/arama/filtre, ayrıntı: üyeler, plan, kullanım grafiği, kota, denetim kaydı, SSO özeti, anahtar sayıları), gerekçe + denetim kayıtlı eylemler (askıya alma/kaldırma, deneme başlatma/uzatma, kota bildirimlerini sıfırlama, üyelerin oturumlarını kapatma, sahibe doğrulama e-postası), sahibin 24 saat/7 gün verdiği destek erişimiyle salt okunur ve her isteği denetlenen destek görünümü (kimlik bilgisi taklidi yok); SaaS modunda askıdaki organizasyona ingest `403 org_suspended`, API salt okunur; plan başına sert host sınırı (yeni host `429 quota_exceeded`, mevcut hostlar etkilenmez) ve kullanıcı sınırı (davet, SSO JIT, SCIM); deneme süreleri (`trial_days`, 7/3/1 gün TR/EN e-postaları, bitişte fallback plan); anormal ingest/yeni org host sayısı/kaynak IP bayrakları (`OPENLOG_SAAS_AUTO_SUSPEND` olmadan otomatik askıya alma yok). Ödeme sağlayıcısı yok (billing `noop`)
  - Veri sahibi talepleri ve durum sayfası — uygulandı (D-107, D-108; `docs/operations/saas.md` §12–§13, api.md "Data export and deletion"/"Status page"): organizasyon dışa aktarma (PostgreSQL verisi JSON + seçilen aralık/sinyallerin telemetrisi gün bazlı `ndjson.gz`, ClickHouse'tan sınırlı ve hız sınırlı akış, lider işi, S3 veya yerel dizin, süreli indirme bağlantısı e-postası, TTL sonunda temizlik) ve kişisel veri dışa aktarma; self-servis hesap silme (tek sahip engeli, denetim kayıtlarında sabit takma ad, oturum/API anahtarı/SSO kimliği/rapor alıcısı temizliği); organizasyon silme (yazılı onay + yeniden doğrulama, anında yumuşak silme, `OPENLOG_ORG_DELETION_GRACE` içinde iptal, ardından tüm `tenant_id` tablolarında partition kapsamlı ClickHouse silme + sıfır satır doğrulaması + PostgreSQL silme + kişisel veri içermeyen silme sertifikası, e-postalar); operatörün gerekçeli silmesi; genel `/status` sayfası ve `GET /api/v1/status` (lider öz-denetimleri, 90 gün uptime, süper yönetici olay/bakım yönetimi Ayarlar → Durum sayfası); PostgreSQL 0070–0071, TR/EN e-postalar. Kalan: gerçek S3 (AWS) ve çok shard'lı kümede silme işinin canlı denemesi; ingest'in silinen kiracıya `401` yerine ayrı `403/410` döndürmesi yapılmadı.
- Kubernetes entegrasyonu (DaemonSet, kube-state) ✅ (D-070, D-071; `deploy/helm/openlog-agent`, `docs/operations/kubernetes.md`, semantic-conventions §7): node agent DaemonSet (pod metadata ile zenginleştirilmiş container metrikleri/logları, `/var/log/pods`, kubelet `/stats/summary`), Lease ile lider seçimli cluster collector (kube-state metrikleri, Kubernetes event'leri log olarak), ClickHouse k8s varlık tabloları, `/api/v1/kubernetes/*`, UI Kubernetes bölümü, k8s alarm şablonları; kind üzerinde OTLP capture ile canlı test (`agents/infra/test/k8s/run.sh`). Kalan: agent chart'ının cluster içindeki gerçek openlog backend'ine karşı denenmesi (UI Kubernetes uç noktaları)
- Java, Python, .NET agent'ları
  - Python agent (D-073; `agents/python`, PyPI `openlog-agent`) ✅: `openlog-instrument` / `openlog_agent.start()`, Django/Flask/FastAPI, requests/httpx/aiohttp, psycopg2/psycopg/asyncpg, PyMySQL/mysqlclient, SQLAlchemy, redis, Celery, gRPC, logging korelasyonu + OTLP log (structlog işlemcisi), runtime metrikleri, gunicorn/Celery prefork fork güvenliği, Go sampler fikstürleri ve ortak DB temizleme vakaları, CI `python-agent` (3.9/3.12/3.13, gerçek PostgreSQL/MySQL/Redis/RabbitMQ/Kafka), PyPI trusted publishing, uWSGI (master/lazy-apps/threads; worker başına post-fork başlatma) ve gevent/eventlet doğrulaması, Tornado/Falcon/Pyramid, aio-pika (RabbitMQ), confluent-kafka/kafka-python/aiokafka instrumentation'ları, `test/localagents` Python demosu (`python-shop`, FastAPI). Kalan: PyPI pending publisher + `pypi` environment kurulumu.
  - Java agent (`agents/java`, D-072) ✅: upstream OpenTelemetry Java agent 2.31.1 + gömülü openlog extension'ı, tek `openlog-javaagent-<sürüm>.jar` (`-javaagent`); `OPENLOG_*`/`openlog.*` yapılandırması, host.id zinciri, container.id, Go örnekleyicisinin portu (Go fikstürleri), Go ile aynı DB temizleme (exporter katmanında), lisans başlığı, gzip; testler: birim + OTLP yakalamayla entegrasyon (Spring Boot 3 MVC/WebFlux, PostgreSQL/MySQL/Hibernate, Lettuce/Jedis, Kafka, gRPC, Logback/Log4j2 korelasyonu ve log ihracı, JVM metrikleri), ek yük ölçümü; CI `java-agent` (JDK 17/21); jar imzalı manifestte (bileşen `java-agent`, format `jar`, `release.yml` `java-agent-jar` işi `release`'ten önce, CI release dry run'da da kontrol); framework testleri: JAX-RS (Jersey 3.1 servlet, Jetty 12), Quarkus 3 (JVM), Vert.x Web 4.5, Micronaut 4 — rota adlandırma + JDBC, JDK 17 ve 21'de yerel olarak geçti (Micronaut'ta ve Grizzly/Jetty handler üzerindeki Jersey'de upstream agent `http.route` koymuyor, belgelendi). Kalan: Maven Central, paylaşılan openlog'da canlı doğrulama.
  - .NET agent (`agents/dotnet`, D-074) ✅: NuGet `OpenLog.Agent` (OpenTelemetry .NET 1.18, `net8.0` + `netstandard2.0`), `builder.Services.AddOpenLog()` / `OpenLogAgent.Start()`, kodsuz kullanım için OTel automatic instrumentation eklentisi (önizleme); `OPENLOG_*` yapılandırması, host.id zinciri, container.id, Go örnekleyicisinin portu (Go fikstürleri) + W3C random bayrağı, Go ile aynı SQL temizleme, lisans başlığı, gzip; testler: xUnit birim + OTLP yakalamayla entegrasyon (ASP.NET Core minimal API/MVC, HttpClient, gRPC, ILogger korelasyonu ve log ihracı, runtime/process metrikleri, PostgreSQL/Npgsql, EF Core, MySqlConnector, StackExchange.Redis; .NET 8 ve 9), ek yük ölçümü; CI `dotnet-agent` (.NET 8/9), release'te NuGet (`NUGET_API_KEY`). Automatic instrumentation eklentisi önizlemeden çıktı: sabitlenmiş OTel .NET automatic instrumentation 1.16.0 (sha256) ile otomatik test (`test/autoinstrumentation.sh`, CI .NET 8/9; eklenti `net/` dizinine kurulur, uygulama dizini çalışmıyor); MassTransit 8.5 (RabbitMQ) ve Confluent.Kafka 2.15 testleri (producer→consumer iz sürekliliği, messaging öznitelikleri); SqlClient testi SQL Server 2022'ye (CI, amd64) ve arm64'te Azure SQL Edge'e karşı; .NET Framework 4.8/ASP.NET 4.x örneği Linux CI'da derleniyor; Windows'ta çalışan test eklendi: CI `dotnet-agent-netfx` (windows-latest) örneği IIS Express'te (yoksa IIS'te) `TelemetryHttpModule` + OpenLog.Agent ile çalıştırıyor, OTLP yakalama konsol uygulamasıyla (`test/OpenLog.NetFx.SmokeTest`, entegrasyon testlerinin yakalama sunucusu) span'leri doğruluyor (`test/netfx/run-iis.ps1`; Linux'ta derleme + site düzeni doğrulandı, ilk Windows CI koşusu betiği doğrulayacak). Kalan: `dotnet-agent-netfx`'in ilk CI koşusunun doğrulanması, paylaşılan openlog'da canlı doğrulama.
- Tail-based sampling — uygulandı (D-075, D-076): `openlog-sampler` (ham traces topic → trace başına tampon → tenant politikası → `traces.sampled` topic), `ot=th` ile birleşik head × tail ağırlıkları, Settings → APM örnekleme (politika + önizleme), Helm Deployment + lag HPA, allinone içinde; `docs/operations/tail-sampling.md`. Kalan: sampler Deployment/PDB/HPA'nın gerçek cluster'da denenmesi (chart render'ı doğrulandı; kind denemesi disk yetmediği için yapılamadı, lag HPA'sı metrics adapter ister)
- Veri ekleme ("Add data") akışı ✅: New Relic benzeri rehberli kurulum sayfası (`/add-data`, derin bağlantılar `/add-data/linux`, `/add-data/apm/node` …, üst çubukta düğme): Linux (install.sh/deb/rpm/tarball), Docker konteyner agent'ı, Kubernetes Helm, Go/Node.js/Python/Java/.NET/PHP APM, sunucu/konteyner/tarayıcı/OpenTelemetry logları, OTel SDK değişkenleri ve Collector YAML'ı, nginx/Redis/MySQL/PostgreSQL entegrasyonları için bu sunucunun uç noktası ve lisans anahtarıyla doldurulmuş kopyala-yapıştır komutlar (saf ve birim testli `buildInstallCommands`, kabuk/YAML kaçışları, URL'de anahtar yok, maskeli gösterim); anahtar adımı: satır içi oluşturma (admin, bir kez gösterilir), yapıştırma (tarayıcıda kalır) veya yer tutucu; "Veri bekleniyor…" doğrulaması (yeni sunucu, APM servisi, küme, log kaydı) ve sorun giderme ipuçları. Backend: `GET /api/v1/onboarding` (uç noktalar, sürüm/kanal, CORS, özellik bayrakları; gizli bilgi yok), `OPENLOG_INGEST_PUBLIC_URL` / `OPENLOG_INGEST_PUBLIC_GRPC_URL`. Hosts/APM/Logs/Kubernetes/Containers boş durumları ve sunucu Servisler sekmesindeki APM kartı (dile göre gerçek ürün adı, `apm_hint.status`, PHP için filo ile kurulum) ilgili karta bağlanır. Dil agent paketleri kayıt defteri olmadan da kurulur: her GitHub sürümüne `openlog-node-<v>.tgz`, `openlog_agent-<v>-py3-none-any.whl` (+ sdist) ve `OpenLog.Agent.<v>.nupkg` (`.sha256` ile, imzalı manifestte `node-agent`/`python-agent`/`dotnet-agent`) eklenir; `GET /api/v1/onboarding` `agent_packages` ile npm/PyPI/nuget.org'da sürümün olup olmadığını (1 saat önbellek, çevrimdışı → unknown) bildirir ve komutlar yalnız `available` olduğunda kayıt defterini, aksi halde GitHub sürüm dosyalarını kullanır (canlı doğrulamada npm/PyPI/NuGet 404 bulgusu). Kalan: gerçek sunucularda uçtan uca kurulum doğrulaması, Windows/macOS sunucular, npm/PyPI/NuGet hesaplarıyla ilk kayıt defteri yayını.

## v1.0 — Tamamlanma kriterleri

Durum (2026-09-14): **geliştirme tamamlandı.** M0–M4 maddeleri kodda; her maddenin ayrıntısı ve kalan küçük notları
yukarıdaki satırlarda. Aşağıdaki üç liste v1.0'ı "tamamlandı" saymak için gerekenleri ayırır: kodda bitenler, uzun süren
doğrulamalar (proje sahibi çalıştırır) ve hesap/altyapı gerektiren operasyonel adımlar.

### Kodda tamamlananlar

- macOS ve Windows host agent'ı ✅ (D-104): darwin amd64/arm64 ve windows amd64/arm64 derlemeleri; yerel metrik/envanter/keşif toplayıcıları (gopsutil, launchd, SCM, kayıt defteri), `launchd_service`/`windows_service` kategorileri, `service` keşif eşleştiricisi (IIS, SQL Server, Homebrew servisleri), macOS birleşik log ve Windows Event Log girdileri, LaunchDaemon ve Windows servisi, servis içinden imza doğrulamalı self-update, `-configure`/`-verify-release`/`-uninstall-service`, `install.sh` macOS desteği, `install.ps1`, amd64 MSI, release/CI macOS ve Windows işleri, "Veri ekle" macOS/Windows kartları, Fleet'te işletim sistemi sütunu.
- Logs Explorer ve Metrics Explorer ✅ (D-118, D-119; api.md "Fields"/"Logs"/"Metrics"/"Saved views", `docs/operations/explorer.md`): SigNoz benzeri dinamik filtre modeli (`internal/querybuilder`: üst düzey alanlar, `attributes.*`/`resource.*`/JSON `body.*`, 16 operatör, VEYA grupları, parametreli SQL), ClickHouse 0080 saatlik öznitelik anahtarı indeksi (materialized view, tip ve kardinalite; indeks boşsa örnekleme), `GET /fields/keys|values`, `POST /logs/query` (dinamik sütunlar, iki yönlü imleç) ve `/logs/aggregate` (histogram, ilk N + other), kayıtlı görünümler (PostgreSQL 0080), tüm OTLP metrikleri için `GET /metrics`, `/metrics/{name}`, `POST /metrics/query` (tipe göre toplama, histogram/summary yüzdelikleri). Metrik alımı düzeltmeleri: exponential histogram kovaları tam sınırlarıyla saklanıyor (önceden scale/offset/negatif kovalar kayıptı), summary quantile'ları (ClickHouse 0081; önceden atılıyordu), değersiz (`no recorded value`) noktalar düşürülüyor. Web: ortak QueryBuilder, Logs Explorer (/logs), Metrics Explorer (/metrics). Testler: Go birim + ClickHouse 25.8 ve PostgreSQL 16 entegrasyon, vitest.
- Altyapı izleme: Linux infra agent (metrik, envanter, keşif, loglar, container/CRI, D-Bus systemd, entegrasyonlar,
  SSH'sız uzaktan yapılandırma, imzalı kendini güncelleme + root `-apply`/`-reconcile`, PHP agent filo kurulumu).
- APM: Go, Node.js, Python, Java, .NET, PHP agent'ları; hata gelen kutusu, servis haritası, log ↔ trace, deployment
  işaretleri, tail-based sampling.
- Tarayıcı izleme (RUM, D-136): tarayıcı SDK'sı, Core Web Vitals, sayfa yükleme zamanlaması, SPA rota değişimleri,
  oturumlar, tarayıcı anahtarları ve kaynak haritaları (minify edilmiş yığınların çözülmesi).
- Synthetic monitoring (D-132): zamanlanmış dışarıdan kontroller ve sonuçları.
- Veritabanı sorgu performansı (D-138): PostgreSQL, MySQL ve SQL Server için ifade istatistikleri, oturum örnekleri
  ve yürütme planları.
- Sürekli profilleme (`docs/contracts/profiles.md`): OTLP profiles alımı, pprof→OTLP çeviren Go agent üreticisi,
  alev grafiği ekranı ve fonksiyon başına self süre.
- Yeni sinyallerin sorgu ve alarm entegrasyonu: `Profile`, `RumPageView`, `RumVital`, `RumSession` ve `DbQuery`
  OQL olay tipleri. `oql` alarm kural tipi herhangi bir OQL sorgusunu koştuğu için bu tipler aynı anda alarma da
  bağlanabilir; sinyal başına ayrı kural tipi yazılmadı.
- Platform: çok kiracılı PostgreSQL + ClickHouse, OQL sorgu dili ve dashboard'lar (filtre, sürüm geçmişi, paylaşım,
  zamanlanmış rapor + PNG renderer), alarmlar ve şablonlar, SSO/SCIM (OIDC, SAML, SLO, back-channel), kota ve kullanım
  ölçümü, katmanlı saklama (S3), Kubernetes entegrasyonu, "Veri ekle" rehberli kurulum, TR/EN arayüz ve e-postalar.
- Dağıtım: Compose (`install-server.sh`), Helm (`openlog`, `openlog-agent`), imzalı release'ler, backend/agent otomatik
  güncelleme, manuel güncelleme düğmeleri.
- Son geliştirme turundaki kısa kontroller: Go build/vet/gofmt, Go birim testleri, web tsc/eslint, hedefli vitest,
  helm lint, compose config, actionlint + shellcheck.

### Uzun süren doğrulamalar (proje sahibi çalıştırır)

- macOS ve Windows host agent'ı (D-104): `ci.yml` `infra-agent-macos`/`infra-agent-windows` işlerinin ve MSI + `install.ps1` kurulum testinin (`agents/infra/packaging/windows/test-install.ps1`) ilk çalışması; gerçek bir macOS/Windows sunucuda self-update (geçiş, onay, rollback) ve Event Log/birleşik log uçtan uca denemesi.
| Doğrulama | Komut |
|---|---|
| Uçtan uca backend + agent | `make e2e` |
| Güncelleme / geri dönüş / N↔N+1 | `make updater-acceptance`, `make mixed-version`, `make stack-demo` |
| TLS | `make tlstest` |
| Katmanlı saklama + yedek/geri yükleme | `TIEREDTEST_BACKUP=1 make tieredtest` |
| Paketler ve kurulum betikleri | `make package-test`, `make install-test` |
| Agent kendini güncelleme (systemd) | `make -C agents/infra test-update-e2e` |
| Web arayüzü | `cd web && npx vitest run && npx playwright test` |
| PostgreSQL entegrasyon | `OPENLOG_TEST_POSTGRES_DSN=… go test -tags integration ./internal/...` |
| SSO (Keycloak) | `test/sso/keycloak_test.go` başındaki komutlar |
| PHP extension matrisi | `make -C agents/php ext-matrix` (+ `ext-asan`, `ext-compat`, `soak`) |
| Dil agent'ları | `make -C agents/node test-integration`, `make -C agents/python test-integration`, `make -C agents/dotnet test-integration test-autoinstrumentation`, `agents/java/test/run.sh` |
| Kubernetes (kind) | `deploy/helm/test/kind-validate.sh` (katmanlı saklama, tail sampling ve agent chart adımları henüz uçtan uca koşmadı) |
| Paylaşılan stack'te canlı doğrulama | `docs/operations/live-validation-2026-09-14.md` kontrol listesi |

### Operasyonel adımlar (hesap veya altyapı gerekir)

- macOS/Windows imzalama (D-104, isteğe bağlı): Apple Developer ID sertifikası ve notarization anahtarı (`APPLE_DEVELOPER_ID_CERT_P12_BASE64`, `APPLE_DEVELOPER_ID_CERT_PASSWORD`, `APPLE_NOTARY_KEY_ID`, `APPLE_NOTARY_ISSUER`, `APPLE_NOTARY_KEY_P8_BASE64`); MSI/exe için Authenticode sertifikası (`WINDOWS_SIGNING_CERT_PFX_BASE64`, `WINDOWS_SIGNING_CERT_PASSWORD`). Sırlar yoksa release işleri uyarıyla imzasız yayınlar.
- Registry yayınları: `NPM_TOKEN`, PyPI pending publisher + `pypi` environment + `PYPI_PUBLISH=true`, `NUGET_API_KEY`;
  Maven Central. Yayın yokken "Veri ekle" komutları GitHub release'indeki paketleri kullanır.
- GHCR: `openlog`, `openlog-infra-agent`, `openlog-renderer` imajları ve (isteğe bağlı `HELM_OCI_PUSH=true`) chart
  paketleri public yapılmalı.
- Gerçek sunucuda son kontrol (deb + Docker'da nginx/redis), gerçek Kubernetes cluster'ı, AWS S3 / SSE-KMS / IRSA.
- Ayrılmış makinede ek yük ölçümleri (PHP, Node.js, Java, .NET).
- Okta ve Entra ID ile SSO doğrulaması; Windows CI job'ı `dotnet-agent-netfx` ilk çalışması.

### Kapsam dışı ve bilinen kısıtlar

- Windows/macOS host agent'ı artık kapsamda (D-104; D-102'nin kapsam kararı geçersiz). Linux'a özgü kalanlar: container metrik/logları, PHP agent kurulumu, Kubernetes, journald; arm64 MSI yok (arm64 için zip + `install.ps1`). Ödeme sağlayıcısı entegrasyonu ve SaaS altyapısı proje sahibinin
  kararını bekliyor (`docs/operations/saas.md`).
- Altinity operator 0.27.3: `disableInsecure` ve plaintext Keeper 2181'in kapatılması (operator probe/istemci sertifikası).
- PHP tam modda izleyici kapalıyken ek yük hedefin üstünde (hafif mod `openlog.userland_hooks=0` hedefe yakın).
- NestJS 12'de Nest seviyesinde span yok (upstream); route adları `--import` veya `OpenLogNestInterceptor` ile doğru.
- Tail sampling at-least-once: çökme/rebalance'ta tutulan span'ler çift olabilir (D-076).
