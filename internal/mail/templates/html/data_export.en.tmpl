{{define "content"}}<p>{{if eq .Kind "organization"}}The data export of organization <strong>{{.OrgName}}</strong> you requested is ready ({{.Size}}).{{else}}The export of your personal openlog data you requested is ready ({{.Size}}).{{end}}</p>{{if .Truncated}}<p>The telemetry part reached the export size or row limit and is incomplete; the archive's manifest.json shows where it stopped. Request a shorter time range for the rest.</p>{{end}}<p>Download it before {{date .ExpiresAt}}. Anyone with the link can download the archive until then, so do not forward this e-mail.</p>{{end}}
{{define "linkLabel"}}Download the archive{{end}}
