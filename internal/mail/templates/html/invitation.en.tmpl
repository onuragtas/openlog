{{define "content"}}<p>{{if .Inviter}}{{.Inviter}}{{else}}An administrator{{end}} invited you to join the organization "{{.OrgName}}" on openlog with the role {{.RoleLabel}}.</p><p>The invitation expires on {{date .ExpiresAt}}. If you did not expect it, ignore this e-mail.</p>{{end}}
{{define "linkLabel"}}Accept invitation{{end}}
