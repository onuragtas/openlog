apiVersion: v2
name: openlog
description: >-
  openlog observability platform - `cluster` deployment profile (horizontally scalable
  openlog-ingest / openlog-processor / openlog-api on Kubernetes, with Kafka and ClickHouse
  either external or managed by the Strimzi and Altinity operators).
type: application
# Chart version (SemVer). Bump on every chart change.
version: 0.1.0
# Default image tag for ghcr.io/onuragtas/openlog.
appVersion: "0.1.0"
# preStop `sleep` lifecycle handler and matchLabelKeys in topologySpreadConstraints.
kubeVersion: ">=1.30.0-0"
home: https://github.com/onuragtas/openlog
sources:
  - https://github.com/onuragtas/openlog
keywords:
  - observability
  - apm
  - opentelemetry
  - otlp
  - clickhouse
  - kafka
annotations:
  artifacthub.io/license: AGPL-3.0-only
